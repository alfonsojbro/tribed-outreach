# Mode 7 — Pipeline ops: LinkedIn sequences, the review queue, and the daily run

LinkedIn outreach runs on the self-hosted **LinkedIn MCP** (`linkedin` server, stickerdaniel/linkedin-mcp-server) driving Alfonso's real LinkedIn session, with the **Tribed outreach tracker** as the record of state. This replaced Gojiberry. There is no campaign engine anymore: the daily run IS the campaign engine. Requires the `linkedin` MCP (actions), the Tribed MCP (outreach lead + queue tools, community onboarding for Job D), and the Apify MCP (follower enrichment).

**The queue is the record, and approval is the ship gate.** Nothing is ever sent that was not written to the queue first: every personalized draft goes in with `queue_outreach_drafts` (Tribed MCP) before a message leaves, so the exact copy is on the record either way. A draft reaches `approved` one of two ways:

- **LinkedIn connection notes (`li_dm`) — the run may approve its own.** Under Alfonso's direct-send authorization (2026-08-06), an unattended run calls `approve_outreach_draft` on the drafts it just wrote and ships them in the SAME run, up to a hard daily cap the server enforces. Order is always queue → approve → send → `mark_outreach_draft_shipped`.
- **Everything else — a human approves in the dashboard.** IG DMs go to the VA by hand, email goes through a person, and **X cold DMs (`x_dm`) stay human-approved** — all via the NobleAdmin Review tab (admin.tribed.io, Messaging > Review). The run cannot approve those kinds; `approve_outreach_draft` refuses them. An approved `x_dm` is not shipped by the run: its copy is stamped onto the lead as `data.x_dm` and the hosted x-drip sends it (see Fill 4). `ship_x_outreach_draft` still exists as the manual single-draft escape hatch through the session worker.

Follow-ups stored on a lead by an approved draft were approved with it — they send on schedule without re-approval. Reply drafts (Mode 2) are never auto-approved or auto-sent. Interactive sessions where the user approves samples in chat may send directly.

## How the stack is wired (know this before touching anything)

- **The `linkedin` MCP does the actions.** It automates a logged-in browser session, so calls run sequentially through one browser and long runs are normal. The tools that matter here:
  - `connect_with_person({ linkedin_username, note })` — connection request; the `note` is where the connection note goes. **`connect_unavailable` is almost never "closed to requests."** On creator profiles (most of this ICP) Follow is the primary button and Connect sits under the More menu; the engine opens More and the payload reports `observed_state`, `connect_route` and `more_menu_opened` so you can see which path it took. A `connect_unavailable` (or a bare no-payload error) means Follow-primary until the payload proves otherwise — never reject a draft or mark a lead uninvitable on it alone; retry next run and only give up when `observed_state` actually shows no Connect anywhere.
  - `send_message({ linkedin_username, message, confirm_send: true, profile_urn? })` — DM. Only works once they are a 1st-degree connection. **Send by `linkedin_username` alone.** `profile_urn` is not a sturdier username: it builds the compose URL directly and skips the profile lookup entirely, so a URN belonging to somebody else opens a composer addressed to that person and the send comes back successful. Only pass a `profile_urn` you read from `get_person_profile` in the same run, for that same person. A URN out of the tracker is only as good as the run that wrote it, and runs before 2026-08-15 wrote some wrong ones (see the URN rule below). **A real send can take ~20 minutes to appear in the thread** (measured 2026-08-17): "not in the thread" minutes after `sent: true` is NOT a phantom send. Never resend because a fresh thread read came back empty — poll `data.li_thread_id` after a few minutes if you must know today, otherwise just recheck next run. **A send is proven by `status`, and by nothing else.** The only value that means the message went out is `"sent"`. `send_failed`, `send_unconfirmed`, `recipient_resolution_failed` and `recipient_urn_mismatch` are NOT sends. And `recipient_selected` is not a delivery signal at all — it is set when a row is clicked in the compose recipient PICKER, before the text is typed and before anything is submitted, and the send wrapper returns `send_unconfirmed` WITH `recipient_selected: true` when the browser throws after the submit. See the stamp rule in Job C phase 1 step 2 before writing any `*_sent_at`.
  - `get_inbox({ limit })`, `get_conversation({ linkedin_username | thread_id, index? })`, `search_conversations({ keywords })` — read threads and detect replies. **`index` exists because one person can hold TWO threads** — the organic 1-on-1 at `index: 0` and an InMail at `index: 1` — and every default read only ever sees the first, so a lead carrying `data.li_inmail_sent_at` is not covered until `index: 1` has been read too (see "The InMail escalation"). **None of them paginate.** `get_inbox` returns the 10-20 most recent threads and never clicks "Load more conversations"; `search_conversations` returns 10 rows and ignores any `limit` above that. Reply coverage therefore comes from looking leads up one at a time, not from listing the mailbox — see Job C phase 1 and "Why the reply sweep has three passes". **And an empty lookup proves nothing, on any of them.** A `search_conversations` on a person's NAME returned LinkedIn's own "We didn't find anything with …" empty state for threads that were open in `get_inbox` at that same minute (Nikki Hoxie and Sara Freed, 2026-08-21). Look a lead up with the ladder in Job C phase 1, and never conclude that a thread, or a send, does not exist from a lookup that came back empty.
  - `get_person_profile({ linkedin_username, sections? })` — profile scrape (anchor material, connection degree, URN).
  - `search_people({ keywords, location?, network? })` — prospecting; `network: ["F"]` filters to 1st-degree (useful to confirm acceptance in bulk).
- **The Tribed tracker holds the state.** Every LinkedIn lead is a record in the outreach tracker (`upsert_outreach_leads`, accountId `"digital_university"`, `channel: "li"`). The approved copy lives in the lead's `data` bag: `data.li_note`, `data.li_followup_1`, `data.li_followup_2`, plus `data.li_username` (the publicIdentifier) and `data.li_urn` when known. Always set `externalId` to the LinkedIn publicIdentifier: upsert dedupes on `externalId` + `channel`, so re-running discovery never duplicates a person. Sequence position lives in `stage` plus `nextAction` (what to do) and `nextActionAt` (YYYY-MM-DD, when it's due). The dashboard's ops home renders a **LinkedIn paced sequences** card projected from exactly these fields (invite → follow-up 1 on acceptance → follow-up 2): what the drip will send today, what is parked and why (stale copy, missing note, no username), and which threads a human owns. It is derived, not stored, so it can never disagree with the drip — read it to see the day before it runs. Every send is recorded with `log_outreach_touch`, using `advanceTo` to move the stage. The dashboard (admin.tribed.io) is the source of truth.
- **Two sending sessions, one tracker, and `data.li_account` is the owner field.** The lead pool stays single (`accountId "digital_university"` — splitting it is what the retired `tribed` account got wrong). What is per lead is the SESSION that sends for it, and the whole of that decision lives in one field:

  | `data.li_account` | Owner | Worked by | Sends through |
  |---|---|---|---|
  | absent, or `"digital_university"` | Alfonso's founder session | leg 1, in-run | the `linkedin` MCP (that browser IS the founder session) |
  | `"martin_guerrero"` | Martin's VA session | leg 2, after leg 1 | the hosted drip, via `https://li-worker.tribed.io/mg` |

  **The selection rule, and why it cannot double-work a lead.** Leg 1 works exactly the leads whose `data.li_account` is absent or `"digital_university"`. Leg 2 works exactly the leads whose `data.li_account` is `"martin_guerrero"`. The two predicates are complements over one field, so a lead lands in exactly one leg: none can be picked up twice and none can fall through. Apply the filter to EVERY selection inside a leg — the reply watch list, today's due list, the acceptance sweep, the follow-up 2 list, the approved-draft list — not only where it feels load-bearing. One unfiltered list is how the same coach gets invited from two accounts on the same morning, which is the single most expensive mistake this split can make.

  **Stamped once, at first queue, then frozen.** The field is written when the lead's first invite draft is upserted, and never changed after anything has been sent. Whichever session invited owns that thread forever, because connection degree, thread ids and the composer are all relative to the viewing account: move a lead between sessions after an invite and every stored read on it becomes an answer to a different question. A lead that genuinely must change hands is parked for a human (`li_state: "human"`, `automated: false`), not re-stamped.

  **Never cross the sessions.** The `linkedin` MCP takes no account parameter, so there is no way to make it answer as Martin — a founder-session `get_person_profile` on one of Martin's invitees reads "· 2nd" and that says nothing about whether MARTIN'S invite was accepted, and a founder-session `get_conversation` opens the wrong mailbox entirely. Leg 2 therefore never calls `mcp__linkedin__*`. Its reads come from Martin's own worker (`get_linkedin_session_health({ linkedinAccount: "martin_guerrero" })`) and from what the drip already wrote to the tracker. The same limit applies to the unibox: its LinkedIn sync still reads the founder session only, so a Martin thread never appears there and its absence proves nothing.

  **Budgets are per session** and come from each account's own health read. See "Volume + account safety" below — they are never shared, summed, or carried across.
- **A URN is a person's identity, so it only ever comes from their own profile.** `data.li_urn` may be written from exactly one source: the top-level `profile_urn` field that `get_person_profile({ linkedin_username })` returns for **that** person. Never from a `references` block, a sidebar or "people also viewed" card, a search-result row, a thread participant, or any other `ACoAA…` string that happened to be on the page — those are other people's URNs, and a profile page is full of them. If `get_person_profile` returns no `profile_urn`, the lead has no URN: write nothing. A missing URN costs nothing (the send falls back to the username lookup); a wrong one addresses a stranger. The engine used to read the first compose anchor anywhere in `<main>`, which on any not-yet-connected profile is a sidebar stranger's — that put four wrong URNs on the tracker from the 2026-08-10 run alone (fixed 2026-08-15 in `linkedin-worker/patch_engine.py`, patches `subject-own-urn` and `urn-contradiction-guard`). Leads corrected by hand keep the old value in `data.li_urn_previous_wrong`.
- **Parking a lead for a human is a state, not a date.** When a person takes a lead back (they replied, they asked to be left alone, they are on hold until a date), record the ownership, not just the calendar: `log_outreach_touch` with `automated: false`, and set `data.li_state: "human"` on the lead. The LinkedIn drip acts only on `li_state` `to_invite` / `awaiting_accept` / `await_fu2` and skips anything else or anything carrying `automated: false`, so it never writes to a parked lead. A future `nextActionAt` alone is a snooze, not a park: it only defers the lead, and the day it comes due the drip picks it up again. Göran Hielscher was parked on 2026-07-31 and the drip overwrote his `nextAction` back to "Draft today's copy, then send" on 08-06. Handing a lead back TO the drip takes both fields: set `data.li_state` to a drip state and log a touch with `automated: true`. Reset only one and the lead stays parked.
- **`dedupe_outreach_leads` hard-deletes and its matcher misses real duplicates — never run it with `apply: true`.** On 2026-08-13 the tracker held 39 real duplicate groups (91 records) and the tool found 0 of them; whatever it deletes is gone, not merged. Duplicates are found by hand: match on normalized publicIdentifier UNION normalized name across `li` leads, pick the canonical record, park the loser (`data.li_state: "human"` AND `automated: false` — both, per the parking rule) and stamp it `data.duplicateOf: "<canonical externalId>"` (the human-written field; a `duplicate_of` written by tooling is a different, untrusted field). Prevention beats cleanup: always upsert with `externalId` = publicIdentifier so discovery reruns dedupe on write.
- **Acceptance is READ, never inferred. This is the one rule to get right.** A LinkedIn invite counts as accepted only when you have read a connection degree of **"· 1st"** off the profile top card (`get_person_profile`) or seen the person come back from `search_people` with `network: ["F"]`. Nothing else is evidence:

  | Signal | What it proves about the connection |
  |---|---|
  | The message sent / delivered | Nothing. LinkedIn **Open Profile** messaging works at 2nd degree with no connection at all, and this ICP (large creator and Premium profiles) is full of open profiles. |
  | The composer opened / a composer probe succeeded | Nothing. Every Open Profile 2nd-degree shows a working composer. |
  | `composer_unavailable`, or a send that failed | Nothing, in **either** direction. It is inconclusive about degree — never "they have not accepted", and never a reason to stop re-checking. |
  | They **replied** | Nothing. Steve Dennis and Steven Jordan both replied and were still "· 2nd" on 2026-08-14. |
  | The profile top card reads "· 1st" | They accepted. |
  | `search_people` returns them under `network: ["F"]` | They accepted. |

  Whenever you record an acceptance, record what proved it in the same write: `data.li_degree` (`"1st"`), `data.li_degree_checked_at` (ISO), `data.li_degree_source` (`"profile_top_card"` or `"search_people_network_f"`). **This is on you, not on the tools** (verified 2026-08-17): `li_degree` appears nowhere in the MCP source, so `update_outreach_lead` / `upsert_outreach_leads` accept an `li_accepted_at` or an `await_fu2` with no proof attached and no complaint. Earlier text here claimed the MCP refuses those writes. It does not. Nothing will stop you writing an acceptance you cannot show, which is exactly why the discipline has to be yours: go read the degree, and write the triple in the same call.

  This exists because two runs got it wrong and messaged people who had never connected. The 2026-08-04 run wrote the touch note "invite accepted (confirmed by a working composer), sent follow-up 1" for a cohort; the 2026-08-13 run advanced leads to `await_fu2` reasoning that a message delivered on 08-04 "is only possible at 1st degree". Both are false. Of the six leads given composer-inference acceptance, the five that could be read were **all** "· 2nd" (Eva Steortz, Tina Lewis, Lion Fludd, Steve Dennis, Steven Jordan). The one lead that verified as "· 1st" (Jeffrey Magee) got there by a different path. Two more (Gerald Joseph, Halle Eavelyn) carried `li_accepted_at` stamps their own `li_state_note` already called "known false".

  Repairing one of those: read the real degree and write it with `li_accepted_at: ""` in the SAME call. Nothing refuses a write that records a non-1st degree while leaving the acceptance stamp in place (verified against the MCP source 2026-08-18, same as above) — which is exactly why the clear has to travel in the same call: a false stamp left behind is one the next run will believe.

- **The sequence map** (what replaced the Gojiberry steps):

  | Old Gojiberry step | Now |
  |---|---|
  | 0 — invitation + note | `connect_with_person` with `note` (ships from an approved draft) |
  | 2 — 1st message after connect (follow-up 1) | `send_message` on the first daily run after they accept |
  | 5 — 2nd message (follow-up 2) | `send_message` 3-4 days after follow-up 1 if no reply |
  | 1 / 4 / 6 — emails | Instantly (Mode 6), via an approved `email` draft. The LinkedIn side sends no email. |
  | 3 — profile visit | dropped |
  | — (new 2026-08-22) | ONE InMail 3-4 days after an unaccepted invite, perfect tier only, drip-owned — see "The InMail escalation" below |

- **Nothing sends itself.** A message goes out only when the daily run (or the user) sends it, and only after its draft is on the record and approved — by the run itself for a LinkedIn connection note, by a human for anything else. A missed daily run only delays touches. It never double-sends, because you check the lead's touch log before sending and log every send after.
- **Lead sourcing** (Gojiberry's agents are gone): leads come from the IG-first combined pipeline below, from the user handing over profiles, or from `search_people` prospecting when asked. Everything gets the same ICP gate.

Session plumbing: the server is registered as `linkedin` in NobleAdmin/.mcp.json and keeps its session in `~/.linkedin-mcp/`. If tools start failing with login or session errors, re-authenticate in a terminal with `uvx mcp-server-linkedin@latest --import-from-browser` (imports from the signed-in browser) or `--login`, then retry.

## The review queue (Tribed MCP, the unattended run's ship gate)

Five tools, all on `accountId "digital_university"` (the dashboard of record):

- `queue_outreach_drafts` — write the run's drafts. Each: `kind` (`"li_dm"` | `"ig_dm"` | `"x_dm"` | `"email"`), `leadName`, `handle`, `externalId` (LinkedIn publicIdentifier / IG handle / X handle / email address; the dedupe key), `anchor` (the rule-zero detail), `language`, `messages` (`[{ slot, content }]` — slots `connection_note`, `followup_1`, `followup_2` for li_dm; `dm` + follow-ups for ig_dm; `dm` for x_dm; `email_1`... for email), and `shipTarget` (what shipping needs later: `{ profileUrl, profileUrn? }` / `{ owner }` / `{ handle }` for x_dm / `{ campaignId, email }`). No usable anchor? Queue it anyway with `anchor` omitted — it lands as **held** and a human supplies the anchor, instead of a generic message shipping. Idempotent on (kind, externalId); an approved draft is never overwritten.
- `list_outreach_drafts` — read the queue by status: `pending` | `held` | `approved` | `rejected` | `shipped` | `error`. Auto-approved drafts carry `autoApproved: true` and `autoApprovedAt`, so you can tell the run's own approvals from a human's.
- `approve_outreach_draft` — approve `li_dm` drafts so THIS run can ship them (direct-send authorization). Pass `draftId` for one or `draftIds` for a batch, plus an optional `note` recorded on the draft. The result gives a per-draft verdict and `remainingToday`, so you always know the budget left. The server refuses anything outside the authorization and says why: a non-`li_dm` kind, a draft that is not `pending`, a missing anchor, a missing or over-long `connection_note`, or the daily cap being spent. Refused drafts are untouched and can still be approved by a human. Re-approving an already-approved draft is a no-op success, so a retried run never fails on it. A rejected draft can never be approved.
- `reject_outreach_draft` — retire a draft the run has determined can never ship, with a REQUIRED `reason` stored on the draft and shown in the Review tab (e.g. `"already has a pending LinkedIn invite from 2026-07-23, there is no second invite to send"`). Pass `draftId` for one or `draftIds` for a batch; the result reports each draft separately, so a partial failure is visible. Only `pending` | `held` | `error` can be rejected — an `approved` draft is a human decision the run must not override (reject that one in the dashboard), and `shipping`/`shipped` are final. Re-rejecting an already-rejected draft is a no-op success, so a retried run never fails on it. Use it instead of leaving dead drafts in the queue forever. Rejecting a draft the run auto-approved also hands its slot back, on the same proof rule: either the draft already carries the proof (a prior `mark_outreach_draft_shipped` with `provenNoSend: true`) or you state it here with `provenNoSend: true`. A draft whose last failure was ambiguous keeps its slot. The result carries `approvalSlotsRefunded`.
- `mark_outreach_draft_shipped` — report each ship attempt: `ok: true` with a `detail` of what happened, or `ok: false` with the `error` (the draft resurfaces in the dashboard and retries next run). **On a failure that proved nothing was sent, pass `provenNoSend: true` plus an `errorKind` (`"already_connected"`, `"invite_pending"`, ...) and the draft hands its approval slot back to today's direct-send budget.** The cap is a live query over `autoApprovedAt`, not a stored counter, so without that a draft that could never send holds a slot for the rest of the UTC day — on 2026-08-19 three of five slots went that way (Jennifer Jehl and Brandon Miller already 1st-degree, Jerry Hu with an invite already pending), and the run sent two invites against a budget of five. `provenNoSend` means nothing left the building: already connected so there is no invite to send, or the invite was never submitted. **If the attempt could have sent — it failed at or after the send boundary, timed out, or you simply do not know — leave it off** (or pass `errorKind: "ambiguous"`, which vetoes the refund whatever else you claim). Refunding a send that may be out would push the run past LinkedIn's real invite limits, which is an account-safety problem rather than a counting one. The result is `{ draft, approvalRefund: { refunded, reason?, explanation? } }`; read `approvalRefund.refunded` rather than assuming the budget moved. Reporting the same failure twice moves one slot, and a human's dashboard approval never held one.

The run's self-approval is deliberately narrow, and the server enforces it rather than trusting the prompt. `approve_outreach_draft` covers LinkedIn connection notes and nothing else, only from `pending`, only with a real anchor and a real note inside LinkedIn's length limit, and only within the daily cap. Rejection stays broader on purpose: a run rejecting its own draft can only ever mean less outreach goes out. What a run can never do is override a person — it cannot resurrect a rejected draft, and it cannot un-approve or re-write one a human signed off on. If you find yourself wanting to widen the approve path (another kind, a held draft, a bigger cap), that is a decision for Alfonso, not a workaround to code around.

## Job A — personalize a lead

For each in-ICP lead:

1. Gather the anchor. Best source is the scraped LinkedIn profile via `get_person_profile` (`headline`, `about`, a named program/book, current role; add `sections: "posts"` when the main page gives nothing). IG anchors come from the profile/feed (Mode 1 / instagram.md). Apply **rule zero** — one real detail, never the niche.
2. Write, in the Tribed voice:
   - **Connection note.** Mode 4A rules: opens with their first name, under ~280 characters (confirm the count), one line on their detail, one line hinting something was built for them, no price, no features, no explicit CTA.
   - **Follow-up 1 (case study).** Mode 3 follow-up 1: how a similar coach's branded app worked, one soft question. Honest, no invented names or metrics; a [case study link] placeholder if useful.
   - **Follow-up 2 (video).** Mode 3 follow-up 2: offer a 60-second explainer video of their own demo, [video link] placeholder, lightest CTA or none.
   - (Optional) **emails** — Mode 6 (email.md), pushed to Instantly via an `email` draft, only if email is enabled for the run.
3. Rotate skeletons and CTAs across leads (see core). Never reuse a line across contacts.
4. Ship or queue, depending on who's watching:
   - **Interactive session (user in chat):** sample 3–5 leads for approval, then save to the tracker (`upsert_outreach_leads`, messages in `data.li_note` / `data.li_followup_1` / `data.li_followup_2`, `nextAction: "Send connection request"`) and send within today's budget. Verify the first of a batch with `get_outreach_lead` before doing the rest.
   - **Unattended run:** queue with `queue_outreach_drafts` (`kind "li_dm"`), then `approve_outreach_draft` and ship in the same run — writing the draft first replaces the in-chat sample approval, so the copy is on the record before the invite goes out. Nothing touches the tracker or LinkedIn until the draft is approved and ships.

## Job B — the ICP filter (run before personalizing new leads)

Filter on real audience signal, not role or industry fit:

1. Collect the batch's `li_username`s (publicIdentifiers).
2. Enrich followers with Apify actor `harvestapi/linkedin-profile-scraper`, mode `"Profile details no email ($4 per 1k)"`, passing the publicIdentifiers. It returns `followerCount`, `connectionsCount`, `creator`, `influencer`. Cost ≈ $0.004/lead. (For a handful of leads, `get_person_profile` follower counts do the job without Apify.)
3. **Keep** a lead if `followerCount >= 2000` OR `creator === true` OR `influencer === true`. Everyone else is out of ICP.
4. Out-of-ICP leads are simply not worked. If one is already on the tracker, take it out of the pipeline with `update_outreach_lead`: clear the `nextAction` and note why in `data` (e.g. `data.icp: "out - 800 followers"`).
5. **Tier the keepers: `data.icp_tier` (`"perfect"` | `"standard"`, absent = standard).** Standard gets the normal invite sequence. **Perfect** opts the lead into the stacked sequence and the paid InMail escalation (see "The InMail escalation" below), so it is deliberately strict — ALL of:
   - passes the base gate above, AND `followerCount >= 10000` or `creator`/`influencer` true — a real audience, not just a qualifying one;
   - visibly sells a named program, course, membership, or coaching offer (bio, site, or featured section shows the monetization, so the pitch has something concrete to land on);
   - a post within the last 30 days to comment on — day 0 of the stacked sequence needs one;
   - a usable rule-zero anchor already in hand.
   Write the tier in the same `update_outreach_lead`/upsert that records the ICP verdict, with the evidence beside it (e.g. `data.icp_tier_evidence: "14k followers, sells 'Strong After 40', posted 3d ago"`). When in doubt, standard: a wrongly-perfect lead spends a paid InMail credit, a wrongly-standard one just gets the normal sequence.

## The InMail escalation (perfect tier only)

**Status 2026-08-22: rails built, channel DORMANT.** The founder profile read on 2026-08-22 showed no Premium badge, so the account has NO InMail credits. Nothing InMails until Alfonso buys a plan himself (his purchase to make, never automated) and the credit ledger is seeded. Recommended: Sales Navigator Core (50 InMail credits/month); Premium Business carries only 15 for a bit over half the price, and 15 is under one perfect-tier escalation every two days.

**The agreed flow (Alfonso approved the shape 2026-08-22).** Perfect-tier leads get a stacked sequence: day 0 a comment on a recent post plus the connection request with note, plus a cold email when a verified address on their OWN domain exists. Then, if no acceptance after 3-4 days, ONE InMail. Never while the invite is fresh (an acceptance makes the credit a waste), never stacked in the same hour as another touch, never a second InMail.

**The hourly drip sends it, not the daily run.** The drip's `awaiting_accept` branch (`mcp/src/repos/linkedinDrip.ts`) escalates only when ALL of these hold, and every gate fails toward "no send":

- `data.icp_tier === "perfect"` (Job B step 5)
- founder-owned lead — `data.li_account` absent or `"digital_university"`; the credit pool belongs to the founder plan and Martin's session has no InMail entitlement
- today's degree read still shows no acceptance, and `data.li_invited_at` is 3+ days old
- no `data.li_inmail_sent_at` yet (one shot, ever)
- `data.li_inmail_subject` and `data.li_inmail` present, `data.li_inmail_copy_at` stamped TODAY (same-day authoring, like every send)
- the credit ledger holds a credit, and the organic thread reads positively clean (a reply parks the lead for a human instead of spending a credit on them)

**The daily run's two InMail jobs:**

1. **Author.** For every founder-owned perfect-tier lead in `awaiting_accept` whose invite is 3+ days old and not yet InMailed: write `data.li_inmail_subject` (under 8 words, their work not ours, no clickbait) and `data.li_inmail` (short escalation in the Tribed voice: one concrete value line anchored on their material, a soft close, no price, and no bracketed placeholders — the worker's lint rejects them), then stamp `data.li_inmail_copy_at` ISO-now in the same write. The drip ships it inside its send window.
2. **Keep the ledger true.** `get_outreach_state({ key: "inmail_credit_ledger" })` — value is `{ available, month: "YYYY-MM", plan?, note? }`, possibly as a JSON string. On the first run of a month, or whenever LinkedIn's UI disagrees, read the REAL credit balance off linkedin.com and `set_outreach_state` the corrected value. The drip only decrements; it never refills, and it refuses to send on a missing, stale-month, or empty ledger. Credits renew on the BILLING date and accumulate (up to LinkedIn's rollover cap), which is why a refill is a human read of the real number, never month arithmetic.

**Reply reads on InMailed leads check TWO threads.** An InMail opens a SECOND thread with the same person: `get_conversation({ linkedin_username, index: 0 })` is the organic thread, `index: 1` the InMail one, and a stored `data.li_thread_id` only ever names the thread it was captured from. Any lead carrying `data.li_inmail_sent_at` must have BOTH threads read before any send or any "no reply" conclusion. The drip's own thread checks do this automatically; the daily session passes `index: 1` explicitly. An InMail reply is a reply like any other: park, advance the stage, draft Mode 2.

**Worker rails.** The hosted worker exposes `POST /sendInMail` (subject + body → engine tool `send_inmail`). The engine refuses to type unless the InMail composer's subject field is present (`not_inmail_composer` — the person is likely already 1st-degree, use a plain DM), reports `no_inmail_credits` on LinkedIn's own blocker, charges a daily `inmailsSent` cap (default 2, warm-up ramp 1/1/2/2 by session week, visible in health `caps`/`warmup`/`limits.inmailsPerDay`), and only `status: "sent"` is a send. **The engine automation is UNVERIFIED against a live InMail composer** (authored 2026-08-22 while the account had no plan): after the purchase, the FIRST use must be a supervised `confirm_send: false` dry run (verifies the composer, sends nothing), then one watched real send, before the drip is allowed to spend credits unattended.

## The rails and the morning quota fill (2026-08-24 — the run's first deliverable)

As of 2026-08-24 every outbound rail is a hosted drip on the tribed MCP, and all of them are live: the **LinkedIn drip sends for BOTH sessions** (per-lead routing on `data.li_account` — founder leads through the founder worker, `martin_guerrero` leads through Martin's), the **Instagram workers execute scheduled sequences**, and the **X drip** (`X_DRIP_ACCOUNT=digital_university`) walks due channel-`"x"` leads hourly inside its send window. The rails handle pacing, jitter, caps, challenge detection and send-time gates in code. What no rail can do is write: **a drip only ships what the daily session prepared** — fresh same-day copy on due LinkedIn leads, scheduled IG sequences, staged X leads. An empty or stale queue does not error anywhere. The rail just idles, and the day's ramp is silently forfeited (unused ramp never rolls over).

Alfonso's standing requirement: **the full daily quota goes out on every outreach account, every day.** The measured failure this section exists to prevent (2026-08-24): the founder session had a ramp of 11 invites + 8 messages and 22 due follow-ups — ALL parked on the same-day copy gate, `li_copy_at` 3 days to 3 weeks old — plus 1 due invite. The run authored fresh copy only for Martin's 6 invites, so `martin_guerrero` spent its ramp and the founder session sent ZERO. The X drip was enabled the same day with zero channel-`"x"` leads on the tracker, so it idled too. `martin_guerrero`'s shape that morning — ramp read, invites staged with fresh copy, drip sending — is the shape EVERY rail on EVERY account must be in when the morning run ends.

So the morning run's first deliverable is not sends. It is **feeding the rails**: four fill jobs, one per rail, run for every account, plus the utilization report. Never stop after the first fill that has work, and never treat "the drip is enabled" as "the quota will go out" — check what the drip can actually act on.

### Fill 1 — same-day copy for EVERY due drip-owned LinkedIn lead, both sessions

`get_linkedin_session_health` now returns a `drip` block per session: `dueActionable` (drip-owned leads the drip could act on today), `copyFreshToday` (how many of them already carry today's `li_copy_at`), `lastSendAt`, and `stalled` (true when work is due but nothing has shipped — the parked-on-copy signature). Read it for each session in `configuredAccounts`, on that session's own health call. **`dueActionable` minus `copyFreshToday` is that session's authoring backlog, and the morning run owns ALL of it.** A `stalled: true` is a finding to fix this run and to flag in the report by name, not a status to relay.

Build the authoring list from the tracker: every `channel "li"` lead that is drip-owned (`data.li_state` in `to_invite` / `awaiting_accept` / `await_fu2`, not `automated: false`), due today or overdue, in BOTH sessions' ownership. For each, author what its state needs and stamp `data.li_copy_at` ISO-now **in the same write**:

- `to_invite` → `data.li_note`, from a fresh read of the profile and recent posts (rule zero).
- `awaiting_accept` → `data.li_followup_1`. The drip sends follow-up 1 the same tick it reads an acceptance, so the copy has to be sitting fresh BEFORE anyone knows the acceptance happened. Skipping these because "they haven't accepted yet" starves every acceptance the drip detects that day. (Perfect tier with an invite 3+ days old: also the InMail pair, per "The InMail escalation".)
- `await_fu2` → `data.li_followup_2`.

**Follow-ups included is the whole point.** Authoring only the new invites is exactly the 2026-08-24 failure: invites are the visible, satisfying half of the backlog, and the 22 due follow-ups were the half that actually starved the founder session. Work the authoring list to zero, follow-ups first (they are further down the funnel and worth more).

**Authoring is a re-read, never a ritual re-stamp.** The gate exists because stored copy rots against a moving prospect (the Jen Uschold re-send, the placeholder rot — see the gate's own comment block). For each lead: check the tracker's conversation log and stage for a reply (a replied lead gets parked, not copy), confirm the stored copy is still true (anchor still valid, no placeholders, nothing overtaken by events), then EITHER re-stamp the good copy (`update_outreach_lead`, merge mode) or rewrite it fresh. Blind bulk re-stamping without reading the lead is weakening the gate with extra steps — banned. So is any change to the gate itself: the fix for a stale-copy park is fresh copy daily, never a looser freshness rule.

### Fill 2 — top up each LinkedIn session's invite queue

Per session, from its OWN health read: `limits.invitesPerDay` is the ramp; count that session's due `to_invite` leads that now carry today's copy (Fill 1 covered the existing ones). The gap is how many NEW leads to source (Job B), personalize (Job A), queue, approve, and stage — upsert with `data.li_account` (Martin's explicitly, founder's absent), `li_state: "to_invite"`, the note in `data.li_note`, `li_copy_at` stamped today, `nextActionAt` today. Over-staging costs nothing (the surplus stays `to_invite` and ships tomorrow — but tomorrow it needs a fresh stamp); under-staging forfeits ramp that never rolls over. Assignment rules and the never-sum rule are unchanged from DRAFT and "Volume + account safety".

### Fill 3 — schedule Instagram sequences to spend each account's caps

Per account in `configuredAccounts`, run the two-step IG preflight gate first, unchanged. For each account that clears it, the day budget is `caps` off `get_instagram_session_account_health({ accountId })` — per account, live, never remembered. Then schedule enough `schedule_instagram_lead_sequence` calls to spend the day: each sequence commits 1 DM + 1 comment + (includeFollow ? 1 follow : 0), so the number of sequences to schedule is bounded by `min(caps.dm.remainingToday, caps.comment.remainingToday)`, with `includeFollow` decided per sequence by a genuine ~50/50 flip and never exceeding `caps.follow.remainingToday`. Each call needs the exact post URL, the exact comment, the exact DM, and `anchorPublishedAt` from the scraped post's own timestamp — author the copy this run (the tool's own freshness guard refuses copy that would be stale by DM time). Copy rides the review queue as ever: schedule from approved `ig_dm` drafts, and leave unapproved ones for the dashboard with a nudge. Fewer approved drafts than remaining caps is a report line ("IG cap 3 DMs, 1 approved draft, 2 slots unfilled — approve or source more"), not a license to ship unapproved copy.

### Fill 4 — stage X leads so the x-drip has a queue

The X drip acts on tracker leads with `channel "x"` on `accountId "digital_university"` — zero such leads means the rail idles by construction, whatever its env says.

**The drip is the API rail, not the browser rail, and the session worker's reply ban does NOT reach it.** The drip's public comment is `x.createComment` (`mcp/src/repos/xDrip.ts` → `repos/x.ts`), an authenticated `POST https://api.x.com/2/tweets` on the OAuth connection. The unverified engine is `reply_x_post_session`, which is the Playwright session worker (`repos/xWorker.ts`) and a different code path entirely. **So stage X leads normally — `x_state`, `nextActionAt` and `data.x_comment` included.** Withholding them does not respect the reply ban, it just hides the leads from the drip and forfeits the day (the 2026-08-25 run did exactly this to 6 leads; backfill those fields). Full rail split, gate table and copy rules: references/x.md.

**The drip has no health gate — do not stand Fill 4 down on the session worker's.** `runXDrip` never reads `get_x_session_account_health`, never checks `caps`, and never checks `browserAdapterReady`; a disarmed browser adapter stops the session worker and nothing else. Still read the health call, because it is the only place `caps` and the gates are visible and the triage task needs them — but report a disarmed adapter as the browser rail being down, not the X leg, and keep staging. Genuine stand-down reasons for Fill 4 are the drip being disabled or the tracker being unreachable. One open item to carry: the drip's writes charge nothing against `caps.*`, so if `XConnection/digital_university` is the same handle as the session login (check with `x_connection_status`), two rails are spending one real account's budget with independent ledgers.

Then keep a due queue staged: source X handles the same way the cross-channel bridge works (bio links off IG/LinkedIn leads already in ICP, or discovery), skip-check, and upsert with `channel "x"`, `externalId` = the handle, `data.x_username`, `data.x_comment`, `x_state "to_touch"`, `nextActionAt` today. The drip resolves and CACHES the user id (paid API lookup, once per lead), likes + comments their latest post, follows on its own judgment, and DMs only when `X_DRIP_DM_ENABLED` — DM copy rides the queue as an `x_dm` draft (human-approved kind) and lands on the lead as `data.x_dm`. **`data.x_comment` fires against whatever their MOST RECENT post is at send time, not the post you read** — write it about the person and their body of work, never about one specific post, or it lands as a non sequitur under something newer. Drip pace is ~5 leads per tick, 40 per run, send window 13–23 UTC: a queue of a couple dozen due leads keeps it fed without flooding it.

### The utilization report — cap vs authored vs sent, per account

The daily report's quota block covers every rail on every account, three numbers each: **cap** (today's ramp/caps off that account's own health read), **authored/staged** (what this run prepared — copy stamped, sequences scheduled, leads staged), and **sent** (what has actually shipped, from the tracker's touch log and the health read's counters — for drip-owned sends this run can only report what has gone out so far, so say "staged N, drip sending" rather than claiming the send). Any LinkedIn session whose `drip.stalled` is true gets flagged by name with the reason found and what this run did about it. A rail at zero always carries its denominator and its why: "X: 0 sent, 0 staged, tracker holds 0 channel-x leads" is a finding; a silent zero is how a rail idles for days. Utilization below cap on any account is a named line with the missing ingredient (no approved drafts, no leads, a hold), never a rolled-up total that hides it.

## Job C — the daily pipeline (PREFLIGHT → QUOTA FILL → REPLY BACKLOG → leg 1 SHIP → DRAFT → leg 2 → REPORT)

Run once per day. The run has TWO sending legs, one per LinkedIn session, and they are strictly sequential: leg 1 is Alfonso's founder session and leg 2 is `martin_guerrero`. Pull the day's due work with `list_outreach_leads({ accountId: "digital_university", channel: "li", dueBefore: <today>, activeOnly: true })` — every outreach tool call needs the explicit `accountId` (there is no default account) — the tracker's `nextActionAt` is the scheduler. That reply is one page: it carries `total` and `nextOffset`, so if `hasMore` is true, page through with `offset` before treating the day's due work as covered. Then split that list by owner per the rule above and work each leg's half separately. Inside a leg, order matters: replies first, then advancing existing leads, then approved new invites, then drafting.

### 0. Preflight — one gate per session, and the legs run SEQUENTIALLY

**Sequential is a hard requirement, not a preference.** Both LinkedIn sessions live on the same Hetzner box and share one Chromium host with the Instagram workers. Never run the two legs concurrently, never fan a leg out to a subagent, and never start leg 2 before leg 1 has finished and called `close_session`. Two browsers driven at once on that box is how both sessions wedge together, and a wedged session takes a manual re-login to recover.

1. **Discover the roster, do not remember it.** `get_linkedin_session_health({ linkedinAccount: "digital_university" })` and read `configuredAccounts` off the response. That is every session this MCP can drive, and it is the list to run legs for. A roster carried over from a previous run goes stale the moment an account is added or removed.
2. **A missing `get_linkedin_session_health` tool is a stand-down for the second leg, not a pass.** If the tool is not in the list at all, the MCP build serving this run predates per-account LinkedIn routing and cannot see Martin's session, his ramp, or whether the registry points at his worker. Stand leg 2 down, stage nothing for him, and report "stale MCP build, per-account LinkedIn routing unavailable" by name so `mcp/dist` gets rebuilt and redeployed. Leg 1 proceeds normally on the founder session. Same fail-closed rule as the Instagram gate: never infer one session's health from another's numbers, and never send on an unread ramp.
3. **Read each session's health separately, naming the account every single time.** One call per account with `linkedinAccount` explicit. There is no combined read, and a health response is never valid for any session but the one asked for. Everything downstream — the ramp, the watchdog, the session error, the proxy error — belongs to that one account.
4. **`accountMatches: false` stands that session down.** It means the worker at the mapped url reports holding a DIFFERENT session than the one you asked for, so every send from that leg would be signed by the wrong person. Report it by name, skip that leg entirely, and do not "send one and see".
5. **A `config` error stands that session down, and only that one.** `No LinkedIn worker is configured for account "martin_guerrero"` means `LINKEDIN_WORKERS` is not set on the deployed MCP (only the legacy single-URL vars are). Martin's leg does not run that day. The founder leg is unaffected and proceeds normally. Report the missing config by name so it gets set, and **never fall back to the founder session to get Martin's sends out** — that invites Martin's prospects from Alfonso's account and silently merges the two ramps.
6. **Founder browser preflight, leg 1 only.** One cheap `get_my_profile`. A session/login error stops leg 1 (see session plumbing above) rather than half-running it; it says nothing about Martin's session, which does not use that browser.
7. **Check the UTC day per session before either leg plans a send.** See "Volume + account safety" below. The stamps are independent, so a founder leg that already ran today never blocks Martin's first run of the day, and vice versa.

A leg that is stood down still contributes to the report: name the session, name the step that stopped it, and give the reason. "LinkedIn partial" with no session named is not a report.

### 0.5 QUOTA FILL — feed every rail before any leg sends

Run "The rails and the morning quota fill" above, in full, right after preflight: Fill 1 (same-day copy for every due drip-owned LinkedIn lead, both sessions, follow-ups first), Fill 2 (top up each session's `to_invite` queue to its ramp), Fill 3 (schedule IG sequences to each account's caps), Fill 4 (keep the x-drip's channel-`"x"` queue staged). Authoring and staging are tracker writes plus read-only lookups, so they are safe before the sending legs and they are what the hosted drips spend all day shipping. A session preflight stood down still gets its authoring done when the tracker is reachable — copy stamped today is what lets the drip send the moment the session comes back. Then report utilization per the section's report contract.

### 1.0 The reply backlog — the unibox sweep (both channels, runs before anything else in SHIP)

The unibox is the inbox of record and it already computes this queue for you. `unibox_list_threads({ communityId: "digital_university", state: "open", sort: "oldestWaiting" })` returns ONLY threads with `awaitingReply=true`, longest wait on top. That is the reply queue, not an inbox view. Run it every run, ahead of the LinkedIn leg below, and work it to zero or to the cap.

**This step is cross-channel on purpose, which is why it sits outside the founder-scoped block.** The LinkedIn passes below build their watch list from the TRACKER (leads at `replied` / `interested` / `demo_request`), so they can only reach someone an earlier run already advanced, and they never touch Instagram at all. This step starts from the INBOX, so it finds the thread whether or not a lead record exists and whatever channel it came in on. The two are complements: this sweep seeds the stages that Pass B then works.

1. **Sync Instagram first.** `unibox_sync_instagram({ accountId })` before any read, per the tool's own rule: an unsynced Instagram inbox proves nothing, and a sync carrying `error` means the inbox is UNKNOWN, never empty. `sources.graph` failed alongside `sources.session` ok is the expected steady state until App Review grants Advanced Access, not a fault. Pass `preserveUnread: true` so a read-only sweep does not clear the account's unread badges.
2. **List the queue and page it.** `sort: "oldestWaiting"`, `limit: 100`, then follow `nextCursor` while `truncated` is true. **Read `truncated` and `coverage` before quoting any number in the report** — a null `nextCursor` means this filtered query is exhausted, never that the inbox is. The unibox's LinkedIn sync reads the founder session only, so a `martin_guerrero` thread never appears here and its absence proves nothing.
3. **Triage before drafting.** A thread whose only inbound is an autoresponder or an away-message bot (Instagram creator auto-replies, "thanks for reaching out, I'll get back to you") gets `unibox_set_state({ threadId, state: "done", markRead: true })` and nothing else. Never draft a reply to a bot, and never leave one sitting in the queue where it hides the real ones. Name the ones closed this way in the report, so a wrong call is visible rather than silent.
4. **CROSS-CHECK THE TRACKER BEFORE YOU DRAFT ANYTHING. `awaitingReply` is not proof that nobody answered.** For every thread that survives triage, open the lead (`get_outreach_lead` on the thread's `leadRef`, or `list_outreach_leads({ search: "<name>" })` when it carries none) and read the **touch log and `nextActionAt`**, not just the stage. A touch recording a sent reply, or a `nextActionAt` in the future, means the thread is handled and the unibox is simply behind: drop it and do not draft. **The unibox is a lagging view of LinkedIn, not the system of record** (measured 2026-08-24, below). A lead with no record at all is the opposite signal: nobody has ever worked them, and they are the likeliest genuine miss.

5. **Read every real thread in full before writing a word.** `unibox_get_thread({ threadId })`. The listing `snippet` is truncated and routinely cuts off the actual ask. Drafting off the snippet is how a thread that asked for pricing gets a "wanna see it?" nudge back.
6. **Queue one reply draft per person** with `queue_outreach_drafts`: `kind` matching the channel (`li_dm` / `ig_dm` / `x_dm`), `externalId` the SAME dedupe key the cold drafts use (LinkedIn publicIdentifier / IG handle), `slot: "reply"`, `anchor` the thing they actually asked for, `language` matching the language THEY wrote in. `slot` is a free string server-side (`repos/outreachQueue.ts` `draftMessageSchema`), so no MCP change is needed for this. Reply drafts are never auto-approved: `approve_outreach_draft` takes `li_dm` `connection_note` only and refuses everything else, and a reply is a human's call regardless. They stay `pending` for the dashboard.
7. **Advance the lead and stop its sequence.** When the thread carries a `leadRef`, `log_outreach_touch` with `advanceTo: "replied"` (or `"interested"` when the reply shows real interest), `nextAction: "Handle reply (Mode 2)"`, `automated: false` and `data.li_state: "human"`. No `leadRef` and it is a real prospect: upsert the lead first. **Someone in the reply queue must never receive a cold follow-up**, and the sequence is what would send it.
8. **Leave the thread `open`.** Only autoresponders get closed here. A thread with a queued-but-unapproved reply is still awaiting a reply, and closing it on the draft would hide the backlog from the moment the copy was written instead of the moment the message went out.

**Answer the question that was asked.** These are Mode 2 replies (references/replies.md), not nudges. Someone who asked for pricing and data-ownership terms gets pricing and data-ownership terms; someone who asked for a written scope of the build gets the scope. A nudge in answer to a concrete question reads as not having read it, and it burns the warmest thing in the pipeline. Price per the core's price reference: one number for their tier, never the range.

**Cap and cursor.** 25 threads per run, oldest-waiting first, remainder carried on `get_outreach_state({ key: "reply_backlog_cursor" })` / `set_outreach_state`, the same pattern as `reply_sweep_cursor` and `query_rotation`. Record the cap being hit in the report so a growing backlog stays visible instead of silent.

**The Meta 24h window is real.** Each thread carries `windowExpiresAt` (ms epoch). A backlogged Instagram thread is past it, so `unibox_send_message` cannot deliver there at all, and session-read threads carry no Graph recipient id either way (those answer with `send_instagram_dm`). Report expired-window threads as needing a manual send rather than quietly queueing a draft that has no ship path.

#### Why this step exists, and why it must cross-check (measured 2026-08-24 — do not re-derive)

The evening channel-health task found 20 of 79 `digital_university` threads with `awaitingReply=true`, 19 with the last inbound over 24h old, 12 LinkedIn and 8 Instagram, the oldest waiting 32 days, against 34 pending drafts of which **not one was a reply**. Two separate faults produced that, and only one of them is the pipeline's.

**Fault 1, the real pipeline gap.** The run had no step that could find an inbound reply. Phase 1's passes all build their watch list from the TRACKER (leads already at replied/interested/demo_request) and are LinkedIn-only, so Instagram threads were unreachable by construction and LinkedIn ones only qualified if an earlier run had already advanced their stage. "Draft a Mode 2 reply for the user" named no tool, while every concrete `queue_outreach_drafts` instruction lived in phase 2 DRAFT, whose slots are cold-outreach slots. The report counted replies FOUND but never threads still WAITING. That is what this step fixes.

**Fault 2, and it is why step 4 exists.** Most of that backlog was NOT unanswered. Worked thread by thread on 2026-08-24: Tony Omary had been answered on 08-22 with pricing and ownership terms AND sent a live demo link, verified in that same thread id. Nathan May was answered 08-22. Jason Cyrus was answered 08-22 with his demo. Tania Ciesler was answered 08-22. Marnus Marx had been worked continuously through 08-21. Jason Dorland and Monique Caissie were correctly deferred to dates they each named. **Amalia Shakti had soft-declined on 08-23** ("no es lo que yo busco ahora") and the unibox still showed her warm July "gracias por conectar" as the last inbound. Of 13 apparent human threads, exactly **two** were genuinely owed a reply.

So the unibox lags LinkedIn badly. Drip-sent messages appear in it; founder-session sends made through the `linkedin` MCP did not, and inbound is missing too (Amalia's 08-23 decline never landed). A run that drafted straight off `awaitingReply` would have written a second message to four people answered two days earlier and pitched one who had just said no. **That is worse than the backlog it was trying to clear**, which is why step 4 is a hard gate and not a nicety. Treat `awaitingReply=true` as "look at this", never as "nobody replied".

**The LinkedIn leg starts here. Everything from this point in the phase is scoped to founder-owned leads: `data.li_account` absent or `"digital_university"`.** Apply that filter when you build each list below, before any budget arithmetic and before any browser call. A `martin_guerrero` lead reached from here is worked by the wrong account: the invite would go from Alfonso, the degree read answers about Alfonso's network, and the thread read opens Alfonso's mailbox. Leg 2 owns those leads and runs after this phase closes.

1. **Replies.** Three passes, in this order. **The inbox is a recent-window peek, not the sweep** — read "Why the reply sweep has three passes" below before changing any of this.

   **Pass A — the recent window.** `get_inbox({ limit: 50 })`, match conversation names against open LinkedIn leads. This catches today's replies and anyone who wrote in who is not on the tracker at all. It returns only the threads LinkedIn first-paints (10-20, measured 17 on 2026-08-14) and stops at "Load more conversations". Never treat an absence here as "no reply".

   **Pass B — the tracker-driven sweep. This is the one that actually finds replies.** Build the watch list from the tracker, not from the inbox: every founder-owned `li` lead at stage `replied`, `interested` or `demo_request`, plus every founder-owned `li` lead carrying a `data.demo_url` with no send logged after the demo was built (the link was never delivered), plus anything already due today. **Drop every lead carrying `data.li_account: "martin_guerrero"` as you build the list** — their threads are in Martin's mailbox, which this browser cannot see, so a name search from here either misses or lands on the wrong conversation. Order it `demo_request` → `interested` → demo-built-but-never-sent → `replied`, and check each lead's thread:
   - **`data.li_thread_id` stored:** `get_conversation({ thread_id })`. One direct navigation, no sidebar enumeration, nothing else marked read. Always the cheapest path, which is why the id gets stored the first time it is ever seen.
   - **No stored thread id: walk the lookup ladder.** The four lookups are not equals. Ranked by how much they are worth, strongest first:

     1. **Stored `data.li_thread_id`** → `get_conversation({ thread_id })`. Direct navigation. The only lookup that cannot miss, which is why the id is stored the first time it is ever seen.
     2. **The `get_inbox({ limit: 50 })` rows Pass A already returned.** A recent window only, but the rows are in hand and cost nothing to re-scan.
     3. **A CONTENT search on the copy we actually sent** — `search_conversations({ keywords: "<a distinctive phrase>", limit: 10 })`, the phrase lifted out of that lead's own stored `data.li_followup_1` or `data.li_note`. Two to four specific words. This is the strongest lookup available to a lead with no stored id.
     4. **A name search** — `search_conversations({ keywords: "<lead display name>", limit: 2 })`. The weakest. It reaches OLD threads the inbox window cannot (LaTonia Monroe Naylor on 07-21, Bego Lozano ~8 weeks quiet, both resolved this way on 2026-08-14), and that is the whole of what it is good for. It also misses live threads intermittently.

     In practice, run them cheapest-first — stored id, then Pass A's rows, then the name search (one call), then the content search — and **stop at the first HIT**. Take the thread id off `references` (NOT off `sections` — see below), write it straight to `data.li_thread_id`, then `get_conversation({ thread_id })`. The ranking is about what an EMPTY answer is worth, not what order to dial: **a hit at any rung is conclusive, an empty at any rung is worth nothing**, and the ladder is only finished when the content search has also come back empty.

   **Absence is never a finding — hard rule.** An empty lookup says the lookup failed, not that the thread or the send does not exist. So: **never clear a `*_sent_at` stamp, never write a "never delivered" or phantom-send note, and never reset a lead's `li_state` backwards, on a name-search miss.** The CONTENT search on that lead's own stored copy has to come back empty too, and any note you then write must record which lookups were run and what each one returned. Short of that, leave the lead exactly as it is and recheck next run — a deferred check costs one day, a cleared stamp re-sends a message the prospect already has. This cost real data on 2026-08-21; see "An empty lookup is never a finding" below.

   Cap Pass B at **40 thread checks per run** (browser calls are sequential and slow). If the watch list is longer, work it in priority order and carry the remainder with a cursor in `get_outreach_state({ key: "reply_sweep_cursor" })` / `set_outreach_state`, oldest-checked first, exactly like `query_rotation`. Record the cap being hit in the report so a growing backlog is visible instead of silent.

   **Pass C — content sweep, for what the tracker's stages have gone stale about.** One `search_conversations({ keywords: "<term>", limit: 10 })` on a rotated term from our own copy (`demo`, `app`, `built`, `tribed`), same rotation state pattern. Each query returns at most 10 threads, so this is a sampler, not coverage — its job is to surface threads whose lead record says nothing is pending. The 2026-08-14 run of `"demo"` turned up Bruce Wren, Neal Foard and Deborah Ivanoff, none of them in the inbox window.

   **On any reply found, by any pass:** stop that lead's sequence with `log_outreach_touch` (`advanceTo: "replied"`, or `"interested"` when the reply shows real interest; `nextAction: "Handle reply (Mode 2)"`), store `data.li_thread_id` if it is not already there, and draft a Mode 2 reply for the user. Replies are never auto-sent. If the reply shows interest and the lead has no demo yet, build the demo first (Job D) so the Mode 2 draft has something real behind it. **A lead whose demo exists and was never sent is a reply that was already missed** — draft the delivery, do not re-open the sequence.
2. **Acceptances → follow-up 1.** Two selections, and you need both. **The due list alone does not find acceptances** — read "Why acceptance needs its own sweep" below before changing any of this.

   **2a. Today's due leads awaiting acceptance.** Whatever `nextActionAt` says is ready.

   **2b. The acceptance sweep — the oldest unchecked invites, due or not.** `list_outreach_leads({ accountId: "digital_university", channel: "li", activeOnly: true })`, paging until `hasMore` is false. Then, in the run: keep leads whose `data.li_state` is `"awaiting_accept"`, drop anything human-owned (`data.automated === false`, or an `li_state` outside the drip's three), **drop every lead whose `data.li_account` is `"martin_guerrero"`** (degree is relative to the viewing account, so this browser can only answer about Alfonso's network — leg 2 covers those), drop anything already carrying today's `data.li_degree_checked_at`, and order the rest by `data.li_degree_checked_at` **ascending with nulls first** — never-checked leads go first. Check the first **`ACCEPTANCE_SWEEP_BUDGET = 12`** of them. Do not raise that to clear a backlog in one run: 12 per run drains a 76-lead cohort in about six runs, and profile reads are sequential browser calls competing with the day's sends.

   Both selections then run the same check: **read the connection degree** (see the acceptance-proof rule above). Use `search_people({ keywords: "<full name>", network: ["F"] })` **first** — a miss answers "No results found" in two lines, where `get_person_profile` spends thousands of tokens of posts on the same one fact. Reach for the profile read only when a hit needs certainty, or when you also want a fresh anchor or URN. Only a badge reading **"· 1st"**, or a `network: ["F"]` hit, is an acceptance.

   **Stamp every check, accepted or not:** `update_outreach_lead` with `data.li_degree`, `data.li_degree_checked_at` (ISO now) and `data.li_degree_source`. This is the sweep's cursor, not bookkeeping. Without it the same handful of leads get re-read every run while the rest of the backlog is never reached — exactly the state the account was in on 2026-08-17.

   Accepted: record the proof first, then send. `update_outreach_lead` with `data: { li_degree: "1st", li_degree_checked_at: "<ISO now>", li_degree_source: "profile_top_card", li_accepted_at: "<ISO now>" }`. **Then read the thread before writing a word into it:** `get_conversation` (by `data.li_thread_id` if stored, else the Pass B lookup ladder — and an empty search is an UNREADABLE thread, never a clean one). Anything from them in there and follow-up 1 is cancelled — park the lead for a human (`automated: false`, `li_state: "human"`, `advanceTo: "replied"`) and draft a Mode 2 reply instead. A newly-detected acceptance is the single most likely place to drop a template on top of a live conversation, because the acceptance and the reply often arrive together. An unreadable thread defers the send to tomorrow; it never clears it.

   Thread clean: `send_message` for `data.li_followup_1` (by `linkedin_username`; pass `data.li_urn` as `profile_urn` only when the tracker holds one AND this run read it off that same person's own profile — never a stored URN from an older run, see the URN rule above). **Then read the payload's `status` before you write anything.** On `"sent"`: re-read the thread, then `log_outreach_touch` with `nextAction: "Send follow-up 2"`, `nextActionAt` 3-4 days out, `automated: true`, and a note that quotes the status and names what carried the proof. On anything else: no stamp, no advance. The acceptance criterion is next, and it is on you — nothing in this path enforces it.

   **A `*_sent_at` stamp needs `status: "sent"`. Nothing else earns one.** `data.li_followup_1_sent_at` and every other `*_sent_at` is written by the run, not by any tool: the field appears nowhere in the MCP, the functions or the worker, so nothing validates it and nothing will refuse a stamp you had no business writing. The criterion, read off the `send_message` payload:

   | Payload | What it means | What you write |
   |---|---|---|
   | `status: "sent"` | The message went out. | Stamp `*_sent_at`, re-read the thread, advance the stage, log the touch. |
   | `status: "send_failed"` | The attempt died **before** anything was submitted. Nothing was sent. | No stamp, no advance. Retry next run. |
   | `status: "send_unconfirmed"` | The submit WAS issued and then the browser threw. The message may or may not be with them. | No stamp, no advance. **Park for a human** (below). Never resend blind. |
   | `status: "recipient_resolution_failed"` | The compose picker never matched a row. Nothing was sent. | No stamp, no advance. Retry next run. |
   | `status: "recipient_urn_mismatch"` | The `profile_urn` you passed belongs to a different member. Nothing was sent. | No stamp, no advance. Clear `data.li_urn`, re-read it from that person's own profile or send without one. |
   | `recipient_selected: true` | **Nothing about delivery.** | Never cite it as evidence of anything. |

   **`recipient_selected` is a pre-submit signal.** It is set by `_select_message_recipient` when a row is clicked in the compose recipient PICKER — before the text is typed and before the submit — and the send wrapper explicitly returns `status: "send_unconfirmed"` **with** `recipient_selected: true` when the browser throws after the submit was issued (`linkedin-worker/patch_engine.py`, the `OLD_PICK`/`NEW_PICK` patch and the wrapper below it). So the field can be true on a send that never landed. On 2026-08-17 Sara Freed's touch recorded "Send confirmed with `recipient_selected=true`". Her message did land, so the touch was right by accident and wrong on the evidence, and nobody reading it later could tell which. **The touch note quotes the `status` field verbatim** — `status: "sent"` — never "send confirmed", never "delivered", never a paraphrase and never a different field.

   **`send_unconfirmed` parks the lead, it does not retry.** Stamp nothing. Log the touch with `automated: false` AND `data.li_state: "human"` (the parking rule above takes both fields), `nextAction: "Verify follow-up 1 landed before any retry"`, and list it in the report. Verification comes from the THREAD — the Pass B lookup ladder, content search included — never from a second send. A duplicate DM to a real prospect cannot be taken back. And an empty lookup is not permission to resend (see "An empty lookup is never a finding"): unreadable means recheck next run, not send again.

   **After a `"sent"`, re-read the thread and record the result in the touch.** The hourly drip already does exactly this — `mcp/src/repos/linkedinDrip.ts` calls `checkThread` immediately after the send and writes the verdict into the note — and this path has no worker doing it for you. Read `get_conversation` by `data.li_thread_id` when stored, else the ladder, and put both halves in the note with the field that carried each one, so a later reader can audit the claim instead of trusting an adjective:

   > `sent — send_message returned status: "sent"; thread re-read clean via get_conversation on data.li_thread_id 2-NWFh…`

   A thread that does not read back is **not** a phantom send: a real send can take ~20 minutes to appear. Write `thread re-read: not yet visible (get_conversation empty)`, leave the stamp standing, and recheck next run. Only the full ladder — content search on this lead's own stored copy included — coming back empty on a LATER run is worth writing down as a suspected phantom, and even then it parks the lead for a human rather than triggering a resend.

   **The message cap is this session's ramp, not the nominal 25.** Read `limits.messagesPerDay` off THIS leg's `get_linkedin_session_health({ linkedinAccount: "digital_university" })` response before you plan the sends and stop at that number. It is the founder session's allowance alone; Martin's messages come out of Martin's own number in leg 2. `MESSAGE_RAMP = (5, 8, 12, 20)` indexed by week since `warmup.json.sessionStartedAt`, and `login.sh` deletes `warmup.json`, so any re-login resets it to 5/day. Acceptances found beyond the ramp keep their degree stamp and their acceptance stamp, stay in `awaiting_accept`, and send on the next run — a detected acceptance is never lost to a spent budget.

   Reads "· 2nd" or anything else: write `data: { li_degree: "<what it read>", li_degree_checked_at: "<ISO now>", li_degree_source: "profile_top_card" }`, and if the lead already carries `li_accepted_at`, clear it in that same write — that stamp was never true, and leaving it lets the next run believe it. Badge unreadable: stamp nothing and bump `nextActionAt` a day; an unreadable badge is inconclusive, not a negative. A failed `send_message` says nothing about degree in either direction — never read it as "not accepted", just retry tomorrow.

   Pending 21+ days **and read as non-1st this run**: mark `stage: "lost"` and list them in the report for manual withdrawal (there is no MCP invite-withdraw tool, and LinkedIn throttles accounts with too many pending invites). **Never mass-mark a pending cohort lost on age alone.** "Invited 25 days ago" plus "no `li_degree` on the record" is not evidence of anything except that nobody looked. On 2026-08-17 that cohort was 76 leads deep and marking it lost would have discarded the real acceptances inside it. Age decides nothing until the degree has been read.
3. **Due follow-up 2.** Founder-owned leads due today with `nextAction: "Send follow-up 2"`, a stored `data.li_degree` of `"1st"`, and still no reply (verify against the thread with `get_conversation` before sending — the touch log can lag a crashed run): send `data.li_followup_2`, then apply the same stamp rule as step 2 — only `status: "sent"` earns `data.li_followup_2_sent_at`, `send_unconfirmed` parks the lead for a human instead of retrying, and the note quotes the status and the thread re-read — and log it with `nextAction: "Archive if still quiet"` and `nextActionAt` 14 days out. Due again and still quiet: mark `stage: "lost"`. **No stored `li_degree`, or one that is not `"1st"`: do not send the closer.** Re-read the degree as in step 2 and act on what it says. This is a hard gate, not a preference: the hourly drip re-reads the degree before the closer and drops such a lead back to `awaiting_accept` on its next tick (clearing the false `li_accepted_at` with it). Nothing in the MCP stops you writing `await_fu2` without proof, so the gate only holds if you apply it.
4. **Approved drafts.** `list_outreach_drafts({ status: "approved" })` (plus `status: "error"` for retries), per kind, within **this session's** invite budget (`limits.invitesPerDay` from the founder health read, never a number that includes Martin's ramp). Ship only the drafts whose lead is founder-owned; a draft already assigned to `martin_guerrero` is leg 2's, and shipping it here would send Martin's copy from Alfonso's account:
   - **li_dm:** `connect_with_person(<li_username from shipTarget/externalId>, <connection_note>)`, then upsert the lead to the tracker (`channel "li"`, `externalId` = publicIdentifier, messages into `data.li_note` / `data.li_followup_1` / `data.li_followup_2`, `data.li_username`, and `data.li_urn` only if this run read a `profile_urn` off that person's own `get_person_profile`) and `log_outreach_touch` (`advanceTo: "reached"`, `nextAction: "Check acceptance"`, `nextActionAt` tomorrow). Approved drafts beyond today's invite budget stay approved and ship on a later run.
   - **ig_dm:** upsert the lead with `channel "ig"`, `owner` from `shipTarget.owner` (e.g. "Martin"), messages in `data` (`data.ig_dm`, `data.ig_followup_1`, `data.ig_followup_2`), `nextAction "Send IG DM"`, `stage "top"` — the VA sends by hand.
   - **email:** push to the Instantly campaign from `shipTarget` (Instantly auto-sends its sequence).
   After each attempt: `mark_outreach_draft_shipped({ draftId, ok, detail | error })`. **`note_sent: false` is a failure, not a send**, and when it comes with `status: "already_connected"` (or the top card reads "· 1st", or an invite is already pending) nothing was sent at all: report it `ok: false` with `provenNoSend: true` and an `errorKind`, so the slot goes back to today's budget instead of being spent on a person you could never invite. Anything you cannot rule a send out for stays a plain `ok: false`.
   Draft that can never ship rather than failed to ship — the prospect already has a pending invite from an earlier run (`data.li_invited_at` set, `li_state: "awaiting_accept"`, and there is no second invite to send), the profile is gone, or they already replied on another channel: retire it with `reject_outreach_draft({ draftIds: [...], reason: "<the concrete reason>" })` instead of leaving it in the queue for a human to clear by hand. Never reject an `approved` draft; that one is the human's call.

#### Why acceptance needs its own sweep (measured 2026-08-17 — do not re-derive)

The due list is a scheduler, and a scheduler cannot answer "who has accepted?". On 2026-08-17 the account held **83 leads in `awaiting_accept`, 76 of them invited 2026-07-23** — 25 days pending — and all 30 of the ones on page 1 of the due list had **`data.li_degree` unset. Never read once.** The run reported "zero acceptances." Acceptances had in fact come in. Their `data.li_followup_1` was written and approved and simply never sent.

Three mechanics produced that, and the sweep exists because fixing any one alone is not enough:

- **The copy-freshness gate ran ahead of the degree read.** In the hourly drip (`mcp/src/repos/linkedinDrip.ts`) the `li_copy_at` check sat before the branch that resolves the profile, so a lead whose copy was stamped 07-23 was parked "draft today's copy" and returned — its profile never resolved, its degree never read, every tick, for 25 days. **A degree read needs no copy at all.** Gate sends on copy freshness; never gate reads.
- **A lead re-parked at `nextActionAt: today` is permanently due and permanently stuck.** It sorts to the front of the due list (soonest first), fills the page cap, gets re-parked, and repeats. 76 such leads crowd out the genuinely due work behind them while none of them advance. That is why a lead's presence on the due list says nothing about whether it is progressing.
- **A spent message budget used to defer the whole lead.** A tick that had already sent its messages skipped `awaiting_accept` leads entirely, so it learned nothing about who had accepted. Reads cost no quota; only budget the send.

So acceptance checking is now keyed on **the degree stamp, not the calendar**: order by `data.li_degree_checked_at` ascending, nulls first, take a fixed budget, and stamp every check. The stamp is the cursor — it is what makes the sweep resumable across runs and what stops the same few leads absorbing the whole budget forever.

**And never report "zero acceptances" without the denominator.** That phrase is what let this hide for 25 days: it reads as "nobody accepted" when it meant "nobody was asked". The digest says how many degrees were **checked**, how many were unreadable, and how many `awaiting_accept` leads are still unswept.

#### Why the reply sweep has three passes (measured 2026-08-14 — do not re-derive)

Neither messaging tool paginates. Both read the rows LinkedIn happens to have rendered, and the `limit` argument does far less than its name suggests:

| Call | What actually came back |
|---|---|
| `get_inbox({ limit: 50 })` | **17 threads**, oldest Aug 4, with "Load more conversations" still sitting at the bottom of the sidebar. An earlier run the same day got **10**. |
| `search_conversations({ keywords: "demo", limit: 10 })` | 10 threads |
| `search_conversations({ keywords: "demo", limit: 50 })` | **the identical 10 threads.** Above 10, `limit` is a no-op. |
| `search_conversations({ keywords: "<a person's name>", limit: 2 })` | The right thread when it hits, at any age — this is the one lookup that reaches threads older than the inbox window. It also **misses live threads**: see the 2026-08-21 rows. |
| `search_conversations({ keywords: "Nikki Hoxie" \| "Hoxie", limit: 2 })` (2026-08-21) | LinkedIn's own "We didn't find anything with …" empty state, only the "New message" row in `references` — while her thread `2-NDdkMTMzZmMtMjU3ZC00NDZjLTk1ZTEtOTBjNzU4YWMwNDQ1XzEwMA==` was visible in `get_inbox` at that same moment. |
| `search_conversations({ keywords: "Rebuild Strong" })` (2026-08-21) | That same Hoxie thread, immediately. A phrase from the message body found what her name could not. |

The engine source says why. `get_inbox` scrolls the sidebar `limit // 10` times and then reads the rendered rows, but **nothing in the engine ever clicks "Load more conversations"** — the sidebar paginates by button, not by scroll, so the scroll walks to the button and stops. `search_conversations` does not scroll at all; it navigates to `/messaging/?searchTerm=…` and enumerates first paint, which is 10 rows. For both, `limit` caps how many already-rendered rows get click-visited, and cannot load a row LinkedIn has not drawn.

So coverage cannot come from the mailbox. It has to come from the tracker naming who to look up, one person at a time — which is Pass B, and which is why Pass B is the load-bearing one. That is the whole fix. Before it, the run's entire reply sweep for 733 LinkedIn leads was a 10-to-17-thread window: Bego Lozano's reply sat unanswered for ~8 weeks, and LaTonia Monroe Naylor said "Ok." on 07-21 with a demo already built at `app.tribed.io/mission-rich` that was never sent.

Three more things that bite:

- **Read `references`, never `sections`.** `search_conversations` extracts the page text *before* the sidebar hydrates, so `sections.search_results` routinely reads "No messages…yet!" on a query that found the thread perfectly well. Every successful lookup on 2026-08-14 looked like that. The thread id is in `references`. Concluding "no thread" from the `sections` text is a false negative every time.
- **Enumerating marks threads read.** Both tools capture thread ids by clicking each row, because LinkedIn's sidebar carries no hrefs and no thread-id attributes. That is why Pass B keeps `limit: 2` on a name lookup and stores `data.li_thread_id` — a stored id makes every later check a direct navigation that touches nothing else.
- **The per-lead check before a send is not a sweep.** Reading a lead's thread with `get_conversation` right before sending it a follow-up works and stays in place (it caught Eva Steortz and Lion Fludd on 2026-08-14 and correctly stopped two closing follow-ups). But it only ever runs on leads the run was already about to touch, so it can never surface a reply from a quiet lead. Do not let it stand in for Pass B.

#### An empty lookup is never a finding (measured 2026-08-21 — this one cost real data)

The guidance here used to say a name search "reaches a thread of any age" and treat it as the authoritative lookup. The true half is that it reaches OLD threads the inbox window cannot. The false half — that it therefore SEES everything — turned two silent misses into a wrong write.

- **Nikki Hoxie.** `search_conversations({ keywords: "Nikki Hoxie", limit: 2 })` and `({ keywords: "Hoxie" })` both returned LinkedIn's explicit empty state. Her thread `2-NDdkMTMzZmMtMjU3ZC00NDZjLTk1ZTEtOTBjNzU4YWMwNDQ1XzEwMA==` was in `get_inbox` at that exact moment, and `search_conversations({ keywords: "Rebuild Strong" })` — a phrase out of the message body — returned it.
- **Sara Freed** (`leadId apOByxFPm0WtXiNkSzi5`) got the same empty name-search result, and the run read it as "follow-up 1 never delivered". Her real `data.li_followup_1_sent_at` stamp was CLEARED, a `li_followup_1_sent_at_false_stamp` note was written, and she was reset to `awaiting_accept` with a nextAction to send follow-up 1 again because "thread is empty". Then a content search on `"another marriage coach"` returned thread `2-NWFhNzYzNzEtMWZhMy00YjAxLTk0ZTAtZTRiYzlhNTBlM2QxXzEwMA==`, holding her stored `data.li_followup_1` verbatim, sent 2026-08-17 4:22 PM. The send was real. One more send and she would have had the same paragraph twice.
- **It is intermittent, not structural.** Kavita Kanakia's name search worked the same day. It is not a display-name problem and not a hydration race either — LinkedIn returned its own explicit empty state, reproducibly, across two spellings. Nobody has a cause. Treat the miss as a property of the tool.

So: a positive hit from any lookup is meaningful. An empty result from any lookup is not evidence of anything. Never clear a `*_sent_at` stamp and never write a phantom-send note until the CONTENT search on the lead's own stored copy has also come back empty, and say in the note which lookups ran.

**Sales Navigator status (rewritten 2026-08-22).** The founder profile read on 2026-08-22 showed NO Premium badge, so the 2026-08-14 claim here that "the account has a seat" was stale (or the seat lapsed): there is no plan and there are no InMail credits until Alfonso buys Sales Navigator himself. Inbound **InMail** threads do appear in the regular inbox (an "InMail GLG | Paid Phone Consultation" thread showed up in Pass A, and the inbox has an InMail filter tab), and `get_conversation({ linkedin_username, index: 1 })` reaches a person's second (InMail) thread, so InMail replies are coverable. Still unverified: whether threads started from a **Sales Navigator seat's own inbox** appear on the regular `/messaging/` surface at all — both messaging tools only ever read `/messaging/`, and nothing on this MCP reaches `/sales/inbox`. Until someone settles it, assume SN-native threads are NOT visible and never work a lead solely through Sales Navigator's inbox (the escalation sends regular InMails from the profile composer precisely so the thread lands in `/messaging/`). **To settle it once a seat exists:** message one person from the SN inbox, then run `get_inbox({ limit: 50 })` and a content search on the sent copy, and record here whether either sees the thread.

#### The agent path has no worker under it (why the send status is on you)

Two things send LinkedIn messages for Tribed, and only one of them is guarded.

| | The hourly drip | The daily run (you) |
|---|---|---|
| Route | `mcp/src/repos/linkedinDrip.ts` → `li-worker.tribed.io` → the engine | `mcp__linkedin__send_message` → `linkedin-worker/run_local_engine.sh` → the engine, **locally** |
| Status check | `linkedin-worker/app/main.py`: `SEND_OK_STATUSES["messagesSent"] == {"sent"}`. Any other status becomes a 422, the quota is refunded, the failure is written, and **no lead state moves at all**. | None. The engine's payload comes straight back to you. |
| Quota accounting | Per account, enforced by the worker. | None. |
| Placeholder lint | The worker's. | None. |
| Post-send proof | `checkThread` re-reads the thread and the verdict goes into the touch note; a `no_thread` verdict writes a "SUSPECTED PHANTOM SEND" touch with `automated: false`. | None. |

`NobleAdmin/.mcp.json` runs `linkedin-worker/run_local_engine.sh`, so the whole middle column is worker code the agent path never calls. The drip **cannot** advance a lead on a status that is not `"sent"`. You can. That asymmetry is the entire reason the stamp rule in Job C phase 1 step 2 has to be applied by hand, and why the touch note has to quote `status` and name the lookup that verified the thread: on this path the note IS the audit trail, because nothing else recorded one.

Sara Freed, 2026-08-17, is the shape of the failure it prevents. Her touch cited `recipient_selected=true` — a field set before the submit — as "send confirmed". The send happened to be real, so nothing broke that day; four days later a reader could not tell a confirmed send from an unconfirmed one, and the sweep cleared her stamp on a name-search miss (see "An empty lookup is never a finding"). Cite the status, re-read the thread, name the field.


### 2. DRAFT

The day's new prospects (from the IG-first pipeline below, `search_people` on rotated queries, or handed over by the user): skip-check each candidate first (`list_outreach_leads({ search: "<handle>" })` — never re-work someone already in the pipeline), ICP filter (Job B), personalize the survivors (Job A), and queue everything with `queue_outreach_drafts`. Anchor-less prospects get queued with no anchor (→ held), never written generic. Rotate discovery searches via `get_outreach_state({ key: "query_rotation" })` (oldest `last_used` first, nulls first) and stamp them with `set_outreach_state` after.

**Never queue a cold draft over an open reply draft.** `queue_outreach_drafts` is idempotent on `(kind, externalId)` and an existing draft in `pending` / `held` / `error` is OVERWRITTEN in place (`repos/outreachQueue.ts`, `findOpenDraft` → the merge transaction); only `approved` and `shipping` are protected. Step 1.0 writes its reply drafts on the same `externalId` the cold drafts use, on purpose, so a reply correctly replaces an unsent cold pitch to someone who has since written in. The dangerous direction is the other one: a cold draft queued here on that key would silently overwrite this morning's reply and nobody would see it happen. So before queueing, drop any candidate that already holds an open draft (`list_outreach_drafts({ status: "pending" })` + `"held"`, matched on `externalId`) in addition to the tracker skip-check. The tracker check alone does not cover it — an inbound-only prospect can have a reply draft and no lead record at all.

**Discovery runs ONCE for the whole run, and ownership is assigned at the end of it.** Sourcing, the ICP gate and personalization are session-agnostic and expensive, so they are not repeated per leg. What is per session is who the lead is then handed to, and that assignment is the moment `data.li_account` is written — the one and only time it is ever set.

**Assign by each session's OWN ramp, taken from its own health read.** Martin's share of today's new leads is `limits.invitesPerDay` from `get_linkedin_session_health({ linkedinAccount: "martin_guerrero" })`, and the founder's share is `limits.invitesPerDay` from the founder's own response. Two numbers, planned separately, never added into a pot and never traded between sessions: on 2026-08-21 that was 5 for the founder and 3 for Martin, so the day is 5 and 3 and there is no 8 anywhere in the arithmetic. Each worker enforces its own cap per UTC day, so an over-assignment costs nothing except a wait — the surplus simply stays `to_invite` and ships on the next day's tick. Under-assigning is the real loss, because an unused ramp does not roll over.

**Which leads go to Martin.** Prefer the plainest, least founder-voiced notes. Anything whose anchor leans on Alfonso's own content, his posts, or a prior Tribed conversation stays on the founder session, because the note has to be true from the mouth of whoever sends it. If Martin's leg was stood down in preflight, assign nobody to him and say so; leaving leads assigned to a session that cannot send is how a backlog silently accumulates against a dead account.

**This phase DECIDES the split; leg 2 WRITES it.** Come out of DRAFT with a named list of the drafts going to Martin and nothing else — no approval, no upsert, no stamp. Leg 2 performs those writes, so there is exactly one place in the run that can set `data.li_account` and exactly one place that can hand a draft to the drip. Everything not on that list is left unstamped and belongs to the founder session by default: do not stamp `"digital_university"` onto founder leads to make the split explicit, because absent already means the founder session and a bulk re-stamp across the tracker is a write nobody can audit.

Then ship the founder session's own first touches, within whatever invite budget leg 1 left of the FOUNDER ramp:

1. `approve_outreach_draft({ draftIds: [<the li_dm drafts just queued>], accountId: "digital_university" })`. Since 2026-08-23 there is NO default pool approval cap: `cap` and `remainingToday` come back `null` unless the call passed its own `dailyCap`, and the real ceiling on invites is each session's worker ramp plus the rolling 7-day guard. Anything refused comes back with its reason; act on it rather than retrying (a `held_needs_anchor` draft needs a real anchor) and never try to route around a refusal.
2. Ship each approved one exactly as in phase 1's `li_dm` step: `connect_with_person`, verify `note_sent`, upsert + `log_outreach_touch`, then `mark_outreach_draft_shipped`.
3. **Retry refused approvals once shipping is done.** With no default pool cap, `daily_cap_reached` only appears if this call passed its own `dailyCap`; the ceiling on invites that actually go out is the worker's ramp and its rolling-week guard, enforced at send time. If a send was refused by the worker (`daily_cap_reached` at the worker, or the rolling 7-day guard), the draft stays approved and ships on a later run. Stop when a pass ships nothing new.

`ig_dm` and `email` drafts are NOT approved here — they stay pending for the dashboard, and the digest nudges the human to the Review tab. Nothing else in this phase sends or touches the tracker.

### 3. Leg 2 — the `martin_guerrero` session

Runs only after leg 1 is finished and `close_session` has been called. Skip the whole leg if preflight stood Martin's session down, and report which step did it.

**This leg reads Martin's session first-hand and stages its sends.** Four read-only tools take a `linkedinAccount` and route to that session's own worker: `get_linkedin_session_health`, `get_linkedin_session_inbox`, `get_linkedin_session_conversation` and `get_linkedin_session_profile`. Name `"martin_guerrero"` on every one of them, every time — the parameter is the SESSION, not the community accountId, and an unmapped id is refused rather than silently falling back to the founder. The separate `linkedin` MCP takes no account at all and is hardwired to the founder session, so it is never the tool for this leg: a founder-session answer about Martin's network is not a partial answer, it is a wrong one. **Sends stay with the drip.** There is deliberately no per-account invite, DM or comment tool, because a raw send out of a VA session would bypass the quota and challenge handling the drip owns. So this leg reads directly, stages sends for the drip out of Martin's own worker, and accounts for what the drip already did honestly.

1. **Health and ramp, named.** `get_linkedin_session_health({ linkedinAccount: "martin_guerrero" })`. This is a second, separate call from leg 1's — never reuse the founder response and never read `configuredAccounts` as if it carried per-account numbers. Take today's budget from `limits` on THIS response: `limits.invitesPerDay`, `limits.messagesPerDay`, `limits.commentsPerDay`. `caps` and `warmup` are there to explain the number in the report (`warmup.sessionAgeDays` tells you how young the session is), not to be recombined into a bigger one. A week-old session sitting around 5 invites/day is at that number, whatever the founder session is at.
2. **UTC-day check for THIS session.** Read `sessions["martin_guerrero"]` out of the `daily_run` state (parse `value`, it is a JSON STRING). Already stamped with today's UTC date means Martin's sends for this UTC day are accounted for and this leg assigns no new invites — it still runs its reads and its reporting. Leg 1's stamp is irrelevant here and vice versa.
3. **Acceptances and replies: read Martin's session directly.** Work the leads where `data.li_account` is `"martin_guerrero"`.

   - **Replies — inbox first.** `get_linkedin_session_inbox({ linkedinAccount: "martin_guerrero", limit: 20 })`. `outbound: false` means the last message in that thread is theirs. **Absence proves nothing**: the inbox is a window on the newest threads and does NOT paginate, so a lead missing from it may simply be older than the window. Never read a short inbox as "nobody replied".
   - **Replies — then the threads the inbox could not cover.** For each watch-list lead not in that window, `get_linkedin_session_conversation({ linkedinAccount: "martin_guerrero", username })`. A content read of the thread itself is the only thing that can disprove it. Compare `senders` against Martin's own name to decide what is inbound — the tool deliberately does not label inbound, because the owner differs per session and a hardcoded name would mislabel every VA thread. A send can surface up to ~20 minutes late, so a thread that looks empty right after a send may not be.
   - **Acceptances — degree, read as Martin.** For each lead still `awaiting_accept`, `get_linkedin_session_profile({ linkedinAccount: "martin_guerrero", username })` and read `firstDegree`. Degree is a property of the PAIR, so it must be read as the account that sent the invite; a founder read says nothing about Martin's invite. `firstDegree` is the ONLY proof of acceptance — a delivered message is not one (an Open Profile 2nd-degree accepts free messages) and `awaiting_accept` is stale in both directions. `firstDegree: false` is weak evidence, not a refusal: it reads the explicit degree badge only, so it can miss an acceptance and the lead simply re-checks tomorrow.
   - **These reads drive Martin's browser**, so they are slow, they queue one at a time, and they share the host with the Instagram workers. Hold the sweep to the same per-run thread cap leg 1 uses and report how many watch-list leads went unchecked, exactly as leg 1 does.
   - **The tracker is still the record of what the DRIP did between runs** — `data.li_degree` / `li_degree_checked_at` / `li_degree_source`, `data.li_state`, the touch log, and leads the drip parked `human` (`automated: false`). Step 4 and the report still read it. What changed is that "did anyone reply to Martin today" no longer has to be inferred from it a day late.
   - **A Martin lead with a reply is still human work, always** — this leg drafts into no one's threads. But the run can now read the reply, so list those leads for the human queue with the actual last message and its sender, not just the drip's touch note.
   - If nothing has moved since the last run, say that plainly, and say how many leads were actually checked. A zero with no denominator is not a report.
4. **Assign and stage today's invites**, exactly as the DRAFT phase describes: approve, upsert with `data.li_account: "martin_guerrero"` + `li_state: "to_invite"` + `li_copy_at` stamped today, then `mark_outreach_draft_shipped({ draftId, ok: true, detail: "queued to martin_guerrero drip" })`. The drip sends inside its send window from Martin's session, and ITS touch log is the record of the actual send. Do not report these as invites sent this run; report them as staged, and let the next run's read of the tracker report what actually went out. **Copy must be same-day.** The drip refuses copy not stamped inside the current UTC day and parks the lead back, so a note authored yesterday for Martin never ships. Re-stamping a good existing `li_note` with `update_outreach_lead` is the fix, not re-authoring it.
5. **Nothing in this leg touches a founder-owned lead**, and nothing in leg 1 touches one of these. If a lead appears in both legs' working sets, that is a bug in the filter, not a lead needing two touches: stop, report it, and send nothing to it from either session.

### 4. REPORT

1. `set_outreach_state({ key: "daily_run", value: { at, perChannel: { discovered, kept, removed, drafted, held, shipped, shipErrors }, sessions: { "digital_university": { at, utcDay, invitesSent, messagesSent, rampInvites }, "martin_guerrero": { at, utcDay, invitesStaged, rampInvites, standDown? } } } })` so the dashboard's Review tab shows the last run. **The `sessions` block is what the next run's per-session UTC-day check reads**, so write an entry for every session a leg ran for, with `utcDay` as today's UTC date (`YYYY-MM-DD` from the ISO timestamp, not the local date — that is the whole point of the field). A session that was stood down gets an entry too, carrying `standDown` with the reason and no send counts: the absence of an entry has to keep meaning "this session has not run today", so never leave a leg unstamped. The `perChannel` block stays exactly as it was and still covers the pool as a whole; `sessions` is additive beside it.
2. **Snapshot the funnel.** Get the stage counts from `get_outreach_lead_counts({ accountId: "digital_university" })` — one call, every lead counted, broken down per channel per stage with no page cap. Never tally `list_outreach_leads` pages for this: that tool returns a page, so a tally of it reports a floor as if it were a total (that is how the 2026-08-09 snapshot came out short — the account had 708 `li` and 513 `ig` leads behind a 500 cap). Map each channel's `byStage` onto the schema-v1 normalized stages and push with `upsert_outreach_metrics` (account, generatedAt, channels[]; `accountId: "digital_university"`). Idempotent on the snapshot date, so re-running the same day is safe.
3. **The reply backlog line — report what is still WAITING, not only what was found.** Off step 1.0's paged listing: threads awaiting a reply right now, split by platform, with the **oldest wait in days** and the count of those older than 24h; how many reply drafts this run queued; how many autoresponder threads it closed, named; how many are past their Meta `windowExpiresAt` and need a manual send; and whether the 25-thread cap was hit with a `reply_backlog_cursor` carry. A run that reports replies found but never threads waiting is how a 19-thread, 32-day backlog grew under daily runs that all reported success (2026-08-24). **If the oldest wait exceeds 7 days, that number leads the digest**, above the quota block: an aged warm reply is worth more than a full day of cold invites, and the report has to say so loudly enough that a human acts on it.
4. `send_admin_report` digest: **one block per sending session first, never a merged LinkedIn total.** For each of founder and `martin_guerrero`, on its own line: health (ok / degraded / stood down with the step and reason), `warmup.sessionAgeDays`, today's ramp as `limits.invitesPerDay` and `limits.messagesPerDay`, and what was spent against it — invites SENT for the founder, invites STAGED for Martin (the drip sends those later, so calling them sent would be a claim the run cannot back), plus for Martin the leads the drip parked `human` since the last run. Write the two ramps as two numbers, never a sum: "founder 9d, 5 invites/day, 4 sent; martin_guerrero 1d, 3 invites/day, 3 staged" is the shape, and a line reading "8 invites/day" is wrong no matter what the totals were. Then: replies found (split by which pass found them — a reply that only Pass B or C caught is one the old inbox-only sweep would have missed, and that count is worth watching), how many watch-list leads went unchecked because Pass B hit its 40-thread cap, **the acceptance line (see below)**, follow-up 2s sent, new invites sent, drafts queued and held (nudge to the dashboard's Review tab), drafts rejected with their reasons, kept vs removed by ICP, demos built, stale invites to withdraw, ship errors.

   **The quota utilization block, every rail, every account (see "The rails and the morning quota fill").** For each LinkedIn session, each IG account, and the X account: cap vs authored/staged vs sent, with any `drip.stalled: true` flagged by name alongside what this run did about it, and every below-cap rail carrying its missing ingredient. Drip-owned sends are reported as staged ("staged N, drip sending"), never claimed as sent.

   **The acceptance line always carries its denominator.** Four numbers, never fewer: degrees **checked** this run (due + sweep, stated separately), **accepted**, **unreadable**, and **`awaiting_accept` leads still unswept** (total in that state minus those carrying today's `li_degree_checked_at`). "Zero acceptances" on its own is banned wording: it reads as "nobody accepted" when the honest meaning is usually "we checked 12 of 83". Write "checked 12 of 83 pending invites, 0 accepted, 1 unreadable, 71 still unswept" instead. Followed by the accepted leads that got follow-up 1, and any acceptance held back by the ramp or by stale copy.

### Volume + account safety (hard limits)

This is browser automation on Alfonso's real LinkedIn account, which LinkedIn's terms prohibit. Volume discipline is what keeps the account alive:

- Max **15 connection requests** and **25 messages** per day — those are ceilings, not budgets, and they are ceilings PER SESSION, not for the two together.
- **The real budget is each account's own ramp, and ramps are never shared, summed, or traded.** Take it from `limits` on that account's `get_linkedin_session_health({ linkedinAccount })` response: `limits.invitesPerDay`, `limits.messagesPerDay`, `limits.commentsPerDay`. `limits` is already the lower of the steady-state `caps` and the young-session `warmup` ramp, so it is the number to plan against; read `caps` and `warmup` only to explain a number in the report. **On 2026-08-21 the founder session was 9 days old at 5 invites/day and `martin_guerrero` was 1 day old at 3 invites/day. That is a 5 and a 3. There is no 8.** (Since 2026-08-23 the invite ramp is (5, 10, 14, 18) by session week with a steady-state cap of 20, so the absolute numbers read higher now; the never-sum rule is unchanged.)
- **`limits` moves a little every day ON PURPOSE — do not diagnose it as drift.** Since 2026-08-23 the worker applies a deterministic per-day ±20% jitter to the invite and DM caps (seeded by UTC day + kind + account) so the day-over-day volume is never a flat line. A founder cap that reads 11 today and 9 tomorrow on the same session age is the jitter working, not a broken ramp or a re-login. Within one UTC day the number is stable across reads. Plan against whatever today's `limits` says, same as always.
- **Invites also carry a rolling 7-day guard of 80 per session** (worker-enforced, env `MAX_INVITES_PER_WEEK`), kept under LinkedIn's ~100/week wall and its 80-90 warning banner. A refusal reading `Rolling 7-day invitesSent cap reached (80)` is this guard, not the daily cap: the session sends nothing more until the trailing week drains, so hold the surplus (`to_invite` keeps), report the guard by name, and never route the overflow through the other session. Lending one session's leftover slots to the other is how a young session gets restricted, and it buys nothing anyway: each worker enforces its own counter, so the extra sends are refused at the far end and the run merely reports numbers that never happened. `login.sh` deletes `warmup.json`, so a re-login drops THAT ONE session back to its floor while the other keeps its ramp — after any re-login, re-read that account's health instead of assuming the ramp you saw yesterday.
- **One run per UTC day, PER SESSION.** The ramp counters are UTC-keyed per account, and an early-morning run in WITA lands in the PREVIOUS UTC day, so two runs the same local day can double a session's ramp. Now that there are two sessions, the trap applies to each independently. `approve_outreach_draft`'s `usedToday` counts the POOL's auto-approvals, not either session's sends (and since 2026-08-23 `cap`/`remainingToday` are `null` unless the call passed its own `dailyCap`), so it is blind to the per-session split. Never treat it as a session budget. Before a leg plans a send, read its own stamp: `get_outreach_state({ key: "daily_run" })`, parse `value` (it is a JSON STRING, and `updatedAt` is when it was written), and compare `sessions["<account>"].utcDay` against today's UTC date. Same UTC day already stamped for that session means that leg's sends are done for the day; the leg still runs its reads, its drafting and its reporting. The stamps are independent, so a founder leg that already ran today never blocks Martin's first run of the day, and vice versa.
- **Never run the two legs at once.** Both sessions share one Chromium host with the Instagram workers. Sequential legs, leg 2 only after leg 1's `close_session`, and never a LinkedIn session inside a subagent.
- Calls queue through one browser session; never try to parallelize, and expect long runs.
- If a tool errors with anything that smells like a checkpoint, captcha, or verification wall, STOP the run immediately and tell the user. Do not retry.
- Sends happen only inside the daily run or on an explicit ask.
- Other channels: max 25 ig_dm handoffs and 50 emails per run. Approved drafts over a cap wait.
- **End every LinkedIn leg with `close_session`**, run finished or run aborted — the automated browser is never left alive after the work is done.

### Instagram preflight — the two-step gate (run before any IG leg)

Instagram sending is gated per account, and the gate is TWO steps: step one reads the hold, step two tests whether the hold is still true. Step two is the one that gets skipped, and skipping it is expensive. On 2026-08-17 a 72h account-wide hold on `digital_university` rested on three profiles, two of them had already recovered, and the stale block idled four days of Instagram sending against a completely free budget and a 45-lead pool.

**Step 1 — read health.** `get_instagram_session_account_health({ accountId })`. Cheap, no browser. Read `configuredAccounts` off the response and run this gate once per account, passing `accountId` explicitly on every call.

**Step 1, same call — the day budget is `caps`, never a remembered number.** The same response carries `caps`: the day ceilings the worker itself enforces, read live from its own `/health` (`caps.source` says so). `caps.follow`, `caps.comment` and `caps.dm` each carry `cap`, `usedToday`, `remainingToday` and `counter` (the worker field the cap is charged against), alongside `caps.day`, `caps.pacing` and `caps.gates`. `caps.day` is the worker's own quota day key, passed through verbatim and never recomputed locally, so compare it as a string rather than assuming which timezone it rolls on. Plan the day off `remainingToday` and nothing else. The caps are PER ACCOUNT: on 2026-08-20 the worker held `digital_university` to 3 follows / 3 comments / 2 DMs and `alfo_briceno` to 2 / 1 / 1, so the "3/3/2" runs had memorised was over-budgeting the second account every single day. `usedToday` counts reserved ATTEMPTS, which is what the worker's cap compares, so an ambiguous or failed attempt has still spent the day. A cap of 0 with `gates.enabled` or `gates.engagementEnabled` false means the worker refuses that action outright, not that the configured number is 0. `pacing` gives the minimum seconds forced between sends and between engagement actions, so the budget cannot be spent in one burst. Exactly one of `caps` or `capsError` is always present. On `capsError`, report it and stand that account's IG budget down: guessing a cap is the failure this field exists to end. If NEITHER is present the server predates the field (it shipped 2026-08-21, commit 0278ec1) - read `serverBuild` on the same response, which names the revision that answered, and rebuild rather than inferring staleness from what is missing.

**Step 1a — a MISSING `queueingBlocked` OR `actionControls` IS A BLOCK, NOT A PASS.** Both fields are unconditional in a current MCP build, so their absence means the build serving this run predates the gate and cannot see a hold that is already recorded server-side. That is exactly what happened on 2026-08-17: health came back yellow/active with neither field while the account was three profiles deep into a recorded suppression. If either field is absent, treat that account as blocked, queue nothing for it, do not run step 2 (a build without the fields has no verify tool either), and report "stale MCP build, gate unavailable" by name so `mcp/dist` gets rebuilt. Every worktree carries its own `mcp/dist`, so a stale build is the normal case, not the exception.

**Step 1b — `queueingBlocked: false` and `overall` not red.** Proceed. Nothing else to do.

**Step 1c — red for a reason that is not action controls** (`sessionStatus` not "active", engagement disabled, any `queueingBlockedReason` other than `action_controls_suppressed`). Stand the account down and report it. Step 2 does not apply: re-probing profiles cannot revive a dead session, and a dead session has to be re-authed by hand.

**Step 2 — `queueingBlocked: true` with `queueingBlockedReason: "action_controls_suppressed"` is NOT a stand-down on its own.** Call `verify_instagram_action_controls({ accountId })` BEFORE accepting the hold. An account-wide hold is assembled from observations days apart, a suppression is per-target and recovers on its own, and nothing else ever re-tests the earlier profiles. The tool opens each handle behind the hold through the account's own browser profile and proxy and READS the action row. It never clicks, never opens an attempt, never touches a quota and never records a suppression, so it is free to run against a blocked account and it does NOT restart the block's window. Health itself names it in `actionControls.verifyBeforeHonouring` when a hold is live. It is slow by nature: a browser launch plus one navigation per handle, up to a couple of minutes.

**Honour the hold only if `after.suppressed` is still true.** Read the result:

- `after.suppressed: false` — the hold had no live evidence behind it. Queueing is clear, the run proceeds normally for that account. `recovered` names the handles that render controls again and `droppedObservations` counts what was pruned. Say so in the report: a hold that verified away is a fact worth having.
- `after.suppressed: true` — the hold is real. Stand the account down, queue nothing, send nothing, retry nothing (every attempt restarts the window). Report by name with `after.clearsAt`, `after.distinctProfiles`, `after.scope` and the `stillSuppressed` handles.
- Verify errored, or the tool does not exist on this build — the hold STANDS. Only positive proof relaxes a hold, never a failure. Same fail-closed rule as step 1a.
- `skipped` is non-empty (more than `MAX_VERIFY_HANDLES` profiles behind the hold) — those observations are unchanged and still count toward `after`, so a hold that survives may only be surviving on unprobed evidence. Run verify again to cover them before treating the block as confirmed.

A run that stands Instagram down must state which step did it: no gate fields (stale build), red for a session reason, or a hold that survived verification. "IG blocked" with no step named is not a report.

### Combined cross-channel daily pipeline (IG-first)

The daily job targets one person across all three channels. Start on Instagram and fan out. All of this happens in the DRAFT phase — every message rides the queue.

**Run the Instagram preflight above first.** If it stands the account down, the IG steps below do not run for that account: no discovery, no IG drafts, no IG handoffs. LinkedIn and email are unaffected by an IG hold and still run.

1. **Discover on Instagram** (WebSearch + Apify `figue/instagram-profile-scraper`): qualified coaches 10k-500k followers with a bio link. Capture `external_url` (bio link), `business_email`, bio, captions. Two Apify traps (2026-08-18): **hashtag search is broken and there is no follower-list actor** — discovery comes from WebSearch/name lists fed to the profile scraper, not from Apify-side search. And always pass **`resultsLimit >= 12`** on posts: with fewer, pinned posts fill the window and an active account reads as dead (stale-looking latest posts), which wrongly fails the liveness check.
2. **IG copy → queue.** Draft a gift-first DM + two follow-ups (Mode 1 + Mode 3). Queue as `kind "ig_dm"`, `externalId` = IG handle, `shipTarget: { owner: "Martin" }`. IG is never auto-sent — after approval the lead lands on the VA's list and the VA sends by hand.
3. **High-confidence match to LinkedIn.** The reliable bridge is the **IG bio link**: if it's a `linkedin.com/in/` URL use it directly; if it's a personal site/Linktree, fetch it and read the LinkedIn link off it; else search by exact name + a corroborating signal (same company / same website domain). If nothing confidently matches, keep the lead **IG-only** — never guess, a wrong match messages the wrong person.
4. **Matched → queue for the LinkedIn sequence.** ICP-gate (Job B), personalize (Job A), queue as `kind "li_dm"`.
5. **Email → queue.** Enrich the email (IG business_email, bio, or LinkedIn contact_info via `get_person_profile`), write a cold email (Mode 6), queue as `kind "email"` with the Instantly campaign in `shipTarget`.
6. **Notify Martin** (WhatsApp) when shipped IG drafts land on his list, and report per channel.

Canonical routing: IG → digital_university pipeline (VA sends); LinkedIn → tracker + `linkedin` MCP (the daily run sends); email → Instantly. The digital_university pipeline (admin.tribed.io) is the dashboard of record — its Review tab is where drafts get approved.

## Job D — build the demo from the conversation

The gift-first promise is that the demo already exists. When a prospect's reply shows interest, make it true before the reply goes back: read the conversation, build their real demo community, and put the link on the lead.

1. **Trigger.** A reply classified as interested (asks to see it, "send it over", a curious question about the app) AND the lead has no `data.demo_url` yet. One demo per prospect, ever: re-read the lead with `get_outreach_lead` and check `list_communities` for an existing community before creating anything. Never recreate or duplicate; demos are real communities in the shared Firebase project and deleting one is a manual decision.
2. **Gather the material.** Three sources, in order of weight:
   - The conversation itself (`get_conversation`): what they call their program, what they reacted to, the words they use for their people. What earned the reply shapes the demo's front door.
   - The LinkedIn profile (`get_person_profile` with `sections: "posts,experience,contact_info"`): voice, method, program names, credentials.
   - Their site or bio link if one exists (fetch it): pricing, program structure, brand colors, testimonials.
3. **Build it with the Tribed MCP.** Call `get_onboarding_guide` FIRST and follow it exactly: extract the author, brand, habits, a starter plan, and questionnaire from the gathered material, then `onboard_community` plus the content tools the guide prescribes. Name and theme it as THEIR brand, not Tribed's. Where the material runs thin, build the smallest demo that still feels like theirs (a tight demo beats an empty shell) and note the gaps in `data.demo_notes`.
4. **Record it.** Write the canonical demo bag on the lead with `update_outreach_lead` in the default merge mode (never `replace`, it drops every other key): `data.demo_url` = `https://tribed.io/c/{communityId}?preview=1`, `data.demo_handle` = the communityId, `data.demo_built_at` = today (YYYY-MM-DD), `data.demo_delivered` = `false`. Then `log_outreach_touch` with `advanceTo: "interested"` and `nextAction: "Send demo link (Mode 2)"`. The Mode 2 draft still uses the `[demo link]` placeholder per the hard rules; the user pastes `data.demo_url` when sending, and `data.demo_delivered` flips to `true` only once it has really gone out.

   **`demo_url` is the only key that counts, and the `?preview=1` is part of it.** Every "needs a demo" query and the admin lead drawer read `data.demo_url` and nothing else, so a demo recorded as `landingUrl` / `demoUrl` / `demoLandingUrl` / `demoCommunityId`, or left only in a touch note, is a live demo that every query reports as missing — on a lead already sitting in a stage that looks finished. Six leads drifted that way before this was pinned down. The bare `tribed.io/c/{slug}` URL serves the coach WEBSITE (website mode), which is dead for a demo-only community, so it is never the link to store or send.
5. **Cap and report.** Max 5 demos per daily run; queue the rest for tomorrow. Report every demo created (lead, community id, demo URL) in the daily report.

## Guardrails

- **Never work a lead from the wrong session.** `data.li_account` decides the owner: absent or `"digital_university"` is leg 1, `"martin_guerrero"` is leg 2, and the filter goes on every list a leg builds. Never send to, degree-read, or thread-read one session's lead from the other, and never fall back to the founder session because Martin's is unavailable — connection degree and threads are relative to the viewing account, so a cross-session read is a wrong answer rather than a partial one. A lead that appears in both legs' working sets is a filter bug: stop and report it, send nothing.
- **Never share, sum, or trade the two ramps.** Each session's budget comes from `limits` on its own `get_linkedin_session_health` response, and one session's unused slots are not the other's to spend. See "Volume + account safety" above.
- **Never infer a LinkedIn acceptance.** Only a read connection degree of "· 1st" counts. A send, a composer, a `composer_unavailable`, and a reply all prove nothing. Record `li_degree` + `li_degree_checked_at` + `li_degree_source` beside every `li_accepted_at`, and never queue follow-up 2 for a lead whose stored `li_degree` is unset or not "1st". See the acceptance-proof rule above.
- **Never honour an Instagram hold without verifying it.** `queueingBlocked: true` is step one of a two-step gate: call `verify_instagram_action_controls` and stand the account down only if `after.suppressed` is still true. The check is read-only on Instagram and does not restart the window. A missing `queueingBlocked` or `actionControls` field is a stale build, so it blocks too. See the Instagram preflight above.
- **Never end a morning run with an unfed rail.** Every account's quota goes out every day, and the drips can only ship what the session prepared: due drip-owned LinkedIn leads without today's `li_copy_at` (follow-ups included), an invite ramp without staged `to_invite` leads, IG caps without scheduled sequences, or an X drip with no channel-`"x"` queue are all the run's failure, reported per rail as cap vs authored vs sent. The fix for a stale-copy park is authoring fresh copy daily — never loosening the same-day gate, and never re-stamping copy without re-reading the lead. See "The rails and the morning quota fill".
- Never work an out-of-ICP lead.
- Never ship a message that fails rule zero — queue it as held (no anchor) instead.
- Unattended runs never send anything that was not queued as a draft first; the queue is the only path. The run may approve its own LinkedIn connection notes within the server-enforced bounds, and only those — IG DMs and email still wait on a person in the dashboard.
- Reply-check before every send; never message someone who already replied — advance their stage instead.
- Sample-then-bulk on any first interactive run against a new batch.
- The connection note has no CTA and no price; pre-demo follow-ups never ask for a call.
- Respect the daily caps. When in doubt, send less.
